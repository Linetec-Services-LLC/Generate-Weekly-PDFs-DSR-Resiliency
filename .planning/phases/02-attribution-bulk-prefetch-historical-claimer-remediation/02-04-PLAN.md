---
phase: 02-attribution-bulk-prefetch-historical-claimer-remediation
plan: 04
type: execute
wave: 4
depends_on: [02, 03]
files_modified:
  - website/docs/runbook/operations.md
  - CLAUDE.md
autonomous: true
requirements: [SPEC-5, SPEC-4]
must_haves:
  truths:
    - "A documented re-activation runbook exists describing the fix -> validate -> flip AUTHORITATIVE=1 -> remediate order (D-09)"
    - "The validation gate is an evidence-based acceptance-criteria run (zero garbage names, O(chunks) HTTP, <=165 min, pytest green) — D-10"
    - "The AUTHORITATIVE=1 flip is documented as a SEPARATE gated operator action, NOT auto-committed in any fix plan (D-11)"
    - "The operator deploy of the lookup_attribution_bulk RPC + PostgREST cache reload is documented as a hard prerequisite (D-01)"
    - "A Living Ledger entry records the bulk-prefetch fix + remediation mode + the prior-incident lineage"
  artifacts:
    - path: "website/docs/runbook/operations.md"
      provides: "Attribution bulk-prefetch re-activation + remediation runbook section"
      contains: "SUPABASE_HASH_STORE_AUTHORITATIVE"
    - path: "CLAUDE.md"
      provides: "Living Ledger entry for Phase 2 attribution bulk-prefetch + remediation"
      contains: "lookup_attribution_bulk"
  key_links:
    - from: "operations.md runbook"
      to: "lookup_attribution_bulk RPC deploy + AUTHORITATIVE=1 flip"
      via: "ordered operator checklist (deploy RPC -> validate -> flip -> remediate)"
      pattern: "lookup_attribution_bulk"
---

<objective>
Ship the operator-facing documentation that closes the phase: a re-activation
runbook (D-09 ordering, D-10 validation gate, D-11 gated flip, D-01 RPC-deploy
prerequisite, D-08 remediation dry-run-first procedure) and a Living Ledger
entry recording the fix + remediation mode + the prior-incident lineage.

Purpose: The code fix (Plans 01-03) lands with `SUPABASE_HASH_STORE_AUTHORITATIVE=0`.
The flip to `1` is a deliberate human-gated operator action AFTER a green
validation run — this plan documents that gate so the premature-flip incident
(67539ec -> corruption -> revert 46cd05d) cannot recur. Covers SPEC Requirement 5
(safe E re-activation) and the operator-procedure half of Requirement 4.
Implements D-09, D-10, D-11, and documents the D-01 operator deploy.

Output: a runbook section in `website/docs/runbook/operations.md` and a
`CLAUDE.md` Living Ledger entry.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-attribution-bulk-prefetch-historical-claimer-remediation/02-SPEC.md
@.planning/phases/02-attribution-bulk-prefetch-historical-claimer-remediation/02-CONTEXT.md
@.planning/phases/02-attribution-bulk-prefetch-historical-claimer-remediation/02-RESEARCH.md
@.planning/phases/02-attribution-bulk-prefetch-historical-claimer-remediation/02-01-SUMMARY.md
@.planning/phases/02-attribution-bulk-prefetch-historical-claimer-remediation/02-02-SUMMARY.md
@.planning/phases/02-attribution-bulk-prefetch-historical-claimer-remediation/02-03-SUMMARY.md
@.claude/rules/documentation-maintenance.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write the E re-activation + remediation runbook in operations.md (D-09, D-10, D-11, D-01)</name>
  <files>website/docs/runbook/operations.md</files>
  <read_first>
    - website/docs/runbook/operations.md (FULL — match the existing operator-workflow page structure and heading style)
    - .claude/rules/documentation-maintenance.md (rewrite-don't-append; organize around operator workflows; state which component owns the flow)
    - .planning/phases/02-attribution-bulk-prefetch-historical-claimer-remediation/02-SPEC.md (Acceptance Criteria — the validation-gate bar)
    - .planning/phases/02-attribution-bulk-prefetch-historical-claimer-remediation/02-CONTEXT.md (D-09, D-10, D-11)
    - The Plan 01/02/03 SUMMARYs (what actually shipped: the RPC, the bulk reader, the scope removal, the remediation flags)
  </read_first>
  <action>
    Add a new operator-workflow section to `website/docs/runbook/operations.md`
    titled "Re-activate Sub-project E (clean filenames + durable hash store)".
    State up front that this flow is owned by the Python billing pipeline
    (`generate_weekly_pdfs.py` + GitHub Actions), per documentation-maintenance.md.

    Document the ORDERED procedure (D-09 — fix -> validate -> flip -> remediate):

    1. **Prerequisite (D-01): deploy the bulk RPC.** The data team applies the
       `lookup_attribution_bulk` `CREATE OR REPLACE` from `billing_audit/schema.sql`
       in the Supabase SQL Editor, then runs `NOTIFY pgrst, 'reload schema';`
       (or Project Settings -> API -> Reload schema cache). Until this is live the
       bulk reader degrades to `fetch_failure`/`no_row` (fail-safe — pipeline
       behaves as today, claimers not yet corrected). Validation MUST run AFTER
       this is deployed.

    2. **Validation gate (D-10) — an evidence-based acceptance run with
       `SUPABASE_HASH_STORE_AUTHORITATIVE=0` still in place.** A real /
       production-equivalent run must demonstrate ALL of:
       - **Zero** generated files named `*_NO_MATCH*` / `*_Unknown_Foreman*` for
         any WR+week+row that has a frozen claimer in `attribution_snapshot`.
       - Attribution Supabase request count is O(distinct bulk queries) (single /
         low-double digits = number of chunks), NOT the ~137k
         `POST /rpc/lookup_attribution` baseline — verified via the run-log HTTP
         count.
       - Runtime within `TIME_BUDGET_MINUTES=165` (no premature graceful-stop /
         "budget exceeded" before generation).
       - `pytest tests/` fully green, including the RED-before/GREEN-after
         historical-claimer regression.
       Capture the evidence (counts, HTTP grep, runtime) before proceeding —
       this is a gate, not a spot-check.

    3. **Flip `AUTHORITATIVE=1` (D-11) — a SEPARATE, explicit operator action.**
       Only after step 2 is green: set
       `SUPABASE_HASH_STORE_AUTHORITATIVE: '1'` in
       `.github/workflows/weekly-excel-generation.yml` `env:` as a one-line change
       in its own commit/PR. Document explicitly that this flip was deliberately
       NOT bundled into the fix PR (the human gate between validation and
       going-live is the whole point — it prevents a repeat of the premature flip
       67539ec -> corruption -> revert 46cd05d / PR #234). The `no_row -> regenerate`
       wave now produces CORRECT clean-named files because the fix resolves real
       claimers.

    4. **Remediate the recent window (D-08, after E is live).** Run the
       remediation mode (from Plan 03) to sweep the old garbage attachments:
       first `REMEDIATE_CLAIMERS='1'` + `REMEDIATION_DRY_RUN='1'` (report-only —
       review the would-delete counts), then re-run with
       `REMEDIATION_DRY_RUN='0'` to execute. Window defaults to
       `REMEDIATION_WINDOW_WEEKS='26'`. Note that remediating AFTER E activation
       means regenerated files are clean-named (no token-to-clean double-churn).
       Deeper history (>~26 weeks) self-heals on next natural edit (out of scope).

    Add a roll-back note: to revert E, set
    `SUPABASE_HASH_STORE_AUTHORITATIVE='0'` (mirrors the 46cd05d mitigation);
    to disable remediation, leave `REMEDIATE_CLAIMERS='0'` (default).

    Follow the rewrite-don't-append rule: integrate cleanly with the page's
    existing sections; do not duplicate the env-var reference (link to
    environment.md for the flag tables). Validate the docs build per
    documentation-maintenance.md if practical.
  </action>
  <verify>
    <automated>python -c "src=open('website/docs/runbook/operations.md',encoding='utf-8').read(); req=['SUPABASE_HASH_STORE_AUTHORITATIVE','lookup_attribution_bulk','REMEDIATE_CLAIMERS','TIME_BUDGET_MINUTES','NOTIFY pgrst','46cd05d']; missing=[r for r in req if r not in src]; assert not missing, 'operations.md missing: '+', '.join(missing); print('runbook OK')"</automated>
  </verify>
  <acceptance_criteria>
    - The inline verify command passes: `website/docs/runbook/operations.md` contains ALL of `SUPABASE_HASH_STORE_AUTHORITATIVE`, `lookup_attribution_bulk`, `REMEDIATE_CLAIMERS`, `TIME_BUDGET_MINUTES`, `NOTIFY pgrst`, and `46cd05d`
    - The ordered procedure documents: (1) RPC deploy + `NOTIFY pgrst, 'reload schema'` prerequisite (D-01), (2) the validation gate with all four evidence checks (D-10), (3) the `AUTHORITATIVE=1` flip as a separate gated operator action explicitly NOT in the fix PR (D-11), (4) the dry-run-first remediation procedure (D-08)
    - The section states the flow is owned by the Python billing pipeline (documentation-maintenance.md)
    - The premature-flip incident lineage (`67539ec` -> `46cd05d` / PR #234) is cited as the reason for the human gate (the `46cd05d` string is asserted by the inline verify)
    - A roll-back note is present (`SUPABASE_HASH_STORE_AUTHORITATIVE='0'`)
  </acceptance_criteria>
  <done>operations.md has a complete, ordered E re-activation + remediation runbook covering D-01/D-08/D-09/D-10/D-11 with the validation-gate evidence bar and the human-gate rationale; the inline string-assertion verify passes.</done>
</task>

<task type="auto">
  <name>Task 2: Append the Phase 2 Living Ledger entry to CLAUDE.md</name>
  <files>CLAUDE.md</files>
  <read_first>
    - CLAUDE.md (the "Living Ledger (Auto-Updated Context)" section at the bottom — match the dated-entry format `[YYYY-MM-DD HH:MM]` and the synthesis style of recent entries, especially [2026-05-26 01:45] the scope hotfix this phase replaces, [2026-05-25 20:55] Sub-project E, and the CR-01 entries)
    - .planning/phases/02-attribution-bulk-prefetch-historical-claimer-remediation/02-01-SUMMARY.md, 02-02-SUMMARY.md, 02-03-SUMMARY.md (what shipped)
  </read_first>
  <action>
    Append a new Living Ledger entry at the END of the `## Living Ledger` section
    in `CLAUDE.md`, timestamped `[2026-05-26 HH:MM]` (executor sets the actual
    commit-time HH:MM — it MUST be a LATER time than the existing `01:45` entry so
    there are two distinct 2026-05-26 entries). Synthesize (what / why / how), not
    a commit dump:

    - **Context:** The [2026-05-26 01:45] `ATTRIBUTION_RESOLUTION_WEEKS=8` scope
      hotfix gated group-KEY/filename formation, not just skipping; composed with
      Sub-project E's `AUTHORITATIVE=1` `no_row -> regenerate` wave (67539ec), it
      regenerated historical groups with garbage claimer names
      (`_User__NO_MATCH` / `_User_Unknown_Foreman`) — 372 of 1,116 files in run
      26439205107 — over real historical attachments. `attribution_snapshot` was
      ~99% populated; the read side just never read it for out-of-scope weeks.
    - **Fix (read-side only):** Replaced the four per-row `lookup_attribution`
      RPC pre-pass sites with a single bulk in-memory prefetch
      (`lookup_attribution_bulk` jsonb RPC reusing the existing `#NO MATCH`-to-NULL
      CASE; `prefetch_attribution` fail-safe reader, distinct op id
      `lookup_attribution_bulk`, chunked at 500 pairs; map-aware
      `resolve_claimer(prefetched_map=)`; on total failure the caller sets B/C HOLD
      DIRECTLY with ZERO additional Supabase calls, D uses-current). Removed
      `ATTRIBUTION_RESOLUTION_WEEKS` entirely (env read, 2 helpers, 4 gates, banner,
      workflow pin, env.md, and the scope test) — the exact-set bulk load makes
      recency-gating obsolete and removes the footgun.
    - **Remediation:** A NEW default-OFF, dry-run-first, isolated
      `run_claimer_remediation` mode sweeps `*_NO_MATCH*` / `*_Unknown_Foreman*`
      attachments in a ~26-week window across TARGET + PPP with the live-identity
      exemption ([2026-05-19 23:45]) when a valid set is supplied (isolated mode
      passes `valid_wr_weeks=None`; name-pattern + window + dry-run-first are the
      guards there).
    - **Sequencing / gate:** fix ships with `AUTHORITATIVE=0`; the flip to `1` is
      a SEPARATE gated operator action after an evidence-based validation run
      (D-09/D-10/D-11) — explicitly NOT auto-committed in the fix PR, preserving
      the human gate that the premature 67539ec flip skipped.
    - **New rules** (state as durable, generalizable rules):
      (1) A recency/scope gate must NEVER sit on group-KEY/filename formation —
      only on skip optimizations; if a value participates in identity, it must be
      resolved for every generated group (the exact-set bulk load is the pattern).
      (2) Any new billing_audit reader reuses `with_retry` + the per-op circuit
      breaker with a DISTINCT op id (op-isolation, extends [2026-04-25 14:00]);
      per-row external I/O over all source rows must be ELIMINATED (bulk load),
      not merely parallelized (extends [2026-05-26 01:45]); and on a bulk
      total-failure the caller applies the per-variant fallback DIRECTLY (HOLD for
      B/C, use-current for D) with ZERO re-invocation of the per-row RPC path.
      (3) A go-live flip that depends on a separate code fix must be a documented,
      human-gated operator action, never bundled into the fix PR (the 67539ec
      lesson).
    - **Tests:** name the new test classes shipped (PrefetchAttributionTests,
      ResolveClaimerMapAwareTests, TestHistoricalClaimerRegression,
      tests/test_claimer_remediation.py) and the final `pytest tests/` count from
      the SUMMARYs.

    Preserve every existing ledger entry verbatim — APPEND only.
  </action>
  <verify>
    <automated>python -c "import re; t=open('CLAUDE.md',encoding='utf-8').read(); req=['lookup_attribution_bulk','ATTRIBUTION_RESOLUTION_WEEKS','run_claimer_remediation','PrefetchAttributionTests']; missing=[r for r in req if r not in t]; assert not missing, 'ledger missing: '+', '.join(missing); n=len(re.findall(r'\[2026-05-26 \d\d:\d\d\]', t)); assert n>=2, 'expected a 2nd 2026-05-26 ledger entry, found '+str(n); print('ledger OK')"</automated>
  </verify>
  <acceptance_criteria>
    - The inline verify command passes: `CLAUDE.md` contains a SECOND `[2026-05-26 HH:MM]` Living Ledger entry (beyond the existing 01:45 one) AND the strings `lookup_attribution_bulk`, `ATTRIBUTION_RESOLUTION_WEEKS`, `run_claimer_remediation`, and `PrefetchAttributionTests`
    - The entry references `lookup_attribution_bulk`, the removal of `ATTRIBUTION_RESOLUTION_WEEKS`, and `run_claimer_remediation` / `REMEDIATE_CLAIMERS`
    - The entry states the three new durable rules (no recency gate on identity formation; distinct-op-id + eliminate-per-row + direct-HOLD-no-re-invocation; human-gated go-live flip)
    - The entry names the new test classes (PrefetchAttributionTests, ResolveClaimerMapAwareTests, TestHistoricalClaimerRegression, tests/test_claimer_remediation.py) and the final `pytest tests/` count
    - Every pre-existing ledger entry is preserved verbatim (APPEND-only; diff shows only additions)
  </acceptance_criteria>
  <done>CLAUDE.md has a synthesized Phase 2 Living Ledger entry (what/why/how + three new rules + test names + count); all prior entries preserved; the inline string + entry-count assertion passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Documentation -> operator action | The runbook drives a human through a destructive flip + a destructive sweep |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-16 | Repudiation / premature go-live | Operator flipping `AUTHORITATIVE=1` before validation | mitigate (D-09/D-10/D-11) | The runbook makes the validation gate a hard, evidence-based prerequisite and documents the flip as a separate human-gated action with the prior-incident lineage as the rationale |
| T-02-17 | Tampering / data loss | Operator running remediation execute without a dry-run review | mitigate (D-08) | The runbook mandates the dry-run-first procedure (report counts -> review -> execute); the code default is dry-run on |
| T-02-18 | Information Disclosure | PII in runbook examples | accept (low) | Examples use placeholder names / counts only; no real foreman names in committed docs |

No HIGH-severity threats. This plan is documentation only — no code or production state changes; it strengthens the human gate that prevents the incident's recurrence.
</threat_model>

<verification>
- `website/docs/runbook/operations.md` contains the ordered re-activation + remediation runbook (D-01/D-08/D-09/D-10/D-11) — inline string-assertion verify passes.
- `CLAUDE.md` has the appended Phase 2 Living Ledger entry (2nd 2026-05-26 entry); all prior entries preserved — inline string + entry-count verify passes.
- `website && npm run build` clean if practical (documentation-maintenance.md docs-build validation).
- No code / workflow / production-state change in this plan (the `AUTHORITATIVE=1` flip remains an operator action, D-11).
</verification>

<success_criteria>
- A complete, ordered E re-activation runbook documents the deploy-RPC -> validate -> flip -> remediate sequence with the D-10 evidence bar and the D-11 human gate.
- The Living Ledger records the fix + remediation mode + the three durable rules + the prior-incident lineage.
- The `AUTHORITATIVE=1` flip is documented as a gated operator follow-up — NOT performed by any plan in this phase.
- Both task verifies are inline string assertions (no executor-created-script dependency, BLOCKER 2).
</success_criteria>

<output>
After completion, create `.planning/phases/02-attribution-bulk-prefetch-historical-claimer-remediation/02-04-SUMMARY.md`
</output>
