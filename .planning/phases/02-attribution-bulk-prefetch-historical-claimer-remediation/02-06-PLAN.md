---
phase: 02-attribution-bulk-prefetch-historical-claimer-remediation
plan: 06
type: execute
wave: 2
depends_on: ["02-05"]
gap_closure: true
files_modified:
  - generate_weekly_pdfs.py
  - .github/workflows/weekly-excel-generation.yml
  - website/docs/runbook/operations.md
  - website/docs/reference/environment.md
  - tests/test_claimer_remediation.py
  - CLAUDE.md
autonomous: true
requirements: [SPEC-4, SPEC-5, SPEC-6]
user_setup: []

must_haves:
  truths:
    - "An operator can activate the destructive remediation sweep through the workflow_dispatch advanced_options parser (the documented path now actually works) (WR-02 closed)"
    - "In the isolated path (valid_wr_weeks=None), a currently-valid _Unknown_Foreman billing file is NOT deleted by an EXECUTE sweep — only _NO_MATCH (a pure Smartsheet error token) is unconditionally swept, _Unknown_Foreman is protected (WR-04 closed)"
    - "The out_of_window counter no longer conflates clean and garbage files — its meaning is unambiguous in the summary line (IN-02 closed)"
    - "The operations.md dry-run quote matches the actual run_claimer_remediation summary-line format (IN-03 closed)"
    - "run_claimer_remediation uses the module-level datetime, not a shadowing local `import datetime as _dt` (IN-04 closed)"
    - "A date-stamped Living Ledger entry summarizing this gap-closure round is committed in the same change set (AUTONOMOUS CLOUD MEMORY rule)"
    - "pytest tests/ passes with a count strictly higher than 02-05's close (SPEC-6)"
  artifacts:
    - path: "generate_weekly_pdfs.py"
      provides: "WR-04 _Unknown_Foreman protection + IN-02 counter clarity + IN-04 datetime cleanup in run_claimer_remediation"
      contains: "_NO_MATCH"
    - path: ".github/workflows/weekly-excel-generation.yml"
      provides: "advanced_options parser handles remediate_claimers / remediation_dry_run / remediation_window_weeks (WR-02 real activation path)"
      contains: "remediate_claimers"
    - path: "website/docs/runbook/operations.md"
      provides: "Step 4 dry-run quote matches actual summary line + real advanced_options activation syntax"
      contains: "run_claimer_remediation [DRY-RUN] complete"
    - path: "CLAUDE.md"
      provides: "Living Ledger gap-closure entry with [YYYY-MM-DD HH:MM] timestamp"
      contains: "ATTRIBUTION_BULK_PREFETCH_FALLBACK"
  key_links:
    - from: ".github/workflows/weekly-excel-generation.yml advanced_options parser"
      to: "REMEDIATE_CLAIMERS / REMEDIATION_DRY_RUN / REMEDIATION_WINDOW_WEEKS env"
      via: "case branches echoing the values into $GITHUB_ENV (and the pinned literals removed so $GITHUB_ENV wins)"
      pattern: "remediate_claimers\\)"
    - from: "generate_weekly_pdfs.py run_claimer_remediation"
      to: "Smartsheet attachment delete"
      via: "garbage-token gate that protects _Unknown_Foreman in the isolated (valid_wr_weeks=None) path"
      pattern: "_Unknown_Foreman"
---

<objective>
Close the remediation-mode findings: make the documented operator activation
path real (WR-02), prevent the isolated EXECUTE sweep from deleting a
currently-valid `_Unknown_Foreman` billing file (WR-04), de-conflate the
out_of_window counter (IN-02), align the runbook dry-run quote to the actual
summary line (IN-03), drop the shadowing `import datetime as _dt` (IN-04), and
commit the date-stamped Living Ledger entry for the whole gap-closure round.

Purpose: `run_claimer_remediation` is DESTRUCTIVE (deletes Smartsheet
attachments). An unreachable documented activation path and a delete-a-valid-file
window are safety hazards; this plan makes activation reachable-and-observable and
shrinks the destructive blast radius to pure-garbage tokens in the isolated path.

Output: a safe, operator-reachable, well-documented remediation mode + the
required Living Ledger memory injection; `generate_weekly_pdfs.py` stays
additive/surgical.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-attribution-bulk-prefetch-historical-claimer-remediation/02-SPEC.md
@.planning/phases/02-attribution-bulk-prefetch-historical-claimer-remediation/02-CONTEXT.md
@.planning/phases/02-attribution-bulk-prefetch-historical-claimer-remediation/02-REVIEW.md
@.planning/phases/02-attribution-bulk-prefetch-historical-claimer-remediation/02-05-PLAN.md
@.claude/rules/documentation-maintenance.md

<interfaces>
<!-- Verified current state (read directly from the codebase 2026-05-26). -->

generate_weekly_pdfs.py — run_claimer_remediation (module-level _GARBAGE_PATTERNS, the function):
```python
_GARBAGE_PATTERNS: tuple[str, ...] = ('_NO_MATCH', '_Unknown_Foreman')

def run_claimer_remediation(client, dry_run, window_weeks, valid_wr_weeks=None):
    import datetime as _dt                  # <-- IN-04: shadows module-level datetime
    _today = _dt.date.today()
    _cutoff = _today - _dt.timedelta(weeks=window_weeks) if window_weeks > 0 else None
    ...
    # window filter increments _total_out_of_window BEFORE the garbage check (IN-02):
    if _cutoff is not None:
        try:
            _week_date = _dt.datetime.strptime(_week_mmddyy, '%m%d%y').date()
            if _week_date < _cutoff:
                _total_out_of_window += 1
                continue
        except (ValueError, TypeError):
            continue
    _is_garbage = any(pat in _name for pat in _GARBAGE_PATTERNS)   # NO_MATCH + Unknown_Foreman both
    if not _is_garbage: continue
    _total_garbage += 1
    if valid_wr_weeks is not None and _identity in valid_wr_weeks:   # exemption skipped when None (WR-04)
        _total_exempted += 1; continue
    # dry-run logs "🔍 [DRY-RUN] would delete garbage attachment ..."
    # else client.Attachments.delete_attachment(_sheet_id, _att_id)
    # summary: "✅ run_claimer_remediation [{_mode}] complete: scanned=.. garbage=.. deleted=.. exempted=.. out_of_window=.."
```
The module-level `datetime` is already imported (used throughout the file).

Dispatch site in main() (~7802-7818): `if REMEDIATE_CLAIMERS:` runs the sweep with
`valid_wr_weeks=None` then `return` (isolated). REMEDIATE_CLAIMERS / REMEDIATION_DRY_RUN
read at ~617-639; REMEDIATION_WINDOW_WEEKS safe-parsed.

.github/workflows/weekly-excel-generation.yml — advanced_options parser (~201-215):
```yaml
      - name: Parse advanced options
        if: github.event.inputs.advanced_options != ''
        run: |
          OPTIONS="${{ github.event.inputs.advanced_options }}"
          for option in $(echo $OPTIONS | tr ',' '\n'); do
            key=$(echo $option | cut -d':' -f1)
            value=$(echo $option | cut -d':' -f2-)
            case $key in
              max_groups) echo "MAX_GROUPS=$value" >> $GITHUB_ENV ;;
              regen_weeks) echo "REGEN_WEEKS=$(echo $value | tr ';' ',')" >> $GITHUB_ENV ;;
              reset_wr_list) echo "RESET_WR_LIST=$(echo $value | tr ';' ',')" >> $GITHUB_ENV ;;
            esac
          done
```
The Phase 2 Plan 03 env pin block (~442-453) currently pins:
`REMEDIATE_CLAIMERS: '0'`, `REMEDIATION_DRY_RUN: '1'`, `REMEDIATION_WINDOW_WEEKS: '26'`
as LITERALS — a literal step env value takes precedence over $GITHUB_ENV, so the
advanced_options parser alone cannot override them. The pins must change to the
`${{ github.event.inputs... || default }}`-or-$GITHUB_ENV form (see Task 1).

website/docs/runbook/operations.md — Step 4 (~133-158): quotes
`would delete N garbage attachments across TARGET/PPP, 0 executed` and shows
`# workflow_dispatch — dry-run pass` YAML snippets that don't map to a real input.

website/docs/reference/environment.md — REMEDIATE_CLAIMERS section (~608-650) says
"Set ... via workflow_dispatch" (currently unreachable).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: WR-02 — make remediation reachable via the advanced_options parser (and unblock the literal env pins)</name>
  <read_first>
    - .github/workflows/weekly-excel-generation.yml: the workflow_dispatch inputs (~23-115, note there are already >10 inputs so adding 3 new top-level inputs is the WRONG approach — extend the advanced_options parser per the CLAUDE.md 10-input-limit convention), the advanced_options parser step (~201-215), and the Phase 2 Plan 03 env pin block (~442-453)
    - website/docs/runbook/operations.md Step 4 (~133-158)
    - website/docs/reference/environment.md REMEDIATE_CLAIMERS section (~608-650)
    - CLAUDE.md — the "GitHub Actions Workflow — advanced_options Parser" section + "GitHub Actions 10-input limit" pitfall (the documented convention that rarely-used controls go through advanced_options, NOT new top-level inputs)
  </read_first>
  <action>
**(a) Extend the advanced_options parser** (NOT new top-level inputs — the
workflow already exceeds 10 inputs; CLAUDE.md mandates the `key:value,key:value`
parser for rarely-used controls). Add three `case` branches:
```bash
              remediate_claimers) echo "REMEDIATE_CLAIMERS=$value" >> $GITHUB_ENV ;;
              remediation_dry_run) echo "REMEDIATION_DRY_RUN=$value" >> $GITHUB_ENV ;;
              remediation_window_weeks) echo "REMEDIATION_WINDOW_WEEKS=$value" >> $GITHUB_ENV ;;
```
Update the `advanced_options` input `description` (~100-104) to mention the new
keys, e.g. append `,remediate_claimers:1,remediation_dry_run:1,remediation_window_weeks:26`
to the example string.

**(b) Make the env pins yield to $GITHUB_ENV.** A literal step-`env:` value
overrides anything echoed to `$GITHUB_ENV`, so the parser branches alone have no
effect while the literals stay. Change the three pins in the Phase 2 Plan 03 env
block (~451-453) from literals to the parser-override-friendly form. The repo's
established pattern is `${{ github.event.inputs.X || 'default' }}`; since these
are driven via advanced_options (not a dedicated input), use the documented
$GITHUB_ENV-precedence form by REMOVING the three literal `env:` keys from the
step env block and instead exporting their DEFAULTS in the "Parse advanced
options" step's surrounding env-setup, OR — simpler and matching MAX_GROUPS et al
which have NO step-env literal — DELETE the three literal pins from the step
`env:` block and let the Python defaults (`'0'`/`'1'`/`'26'` in
generate_weekly_pdfs.py) supply the safe defaults when advanced_options does not
set them. Pick the DELETE-the-literal-pins approach (mirrors how MAX_GROUPS /
REGEN_WEEKS / RESET_WR_LIST are handled — no step-env literal, parser-only). Keep
the explanatory comment block but update it to read: "Activated via
advanced_options (remediate_claimers:1,remediation_dry_run:1,remediation_window_weeks:26);
Python defaults to OFF/dry-run/26wk when unset."

CAUTION: removing the literal pins means the safe default now comes from the
Python `os.getenv(..., '0'/'1'/'26')` defaults — which are already correct
(default OFF, dry-run ON, 26wk). Confirm by reading the env reads in
generate_weekly_pdfs.py (~617-639): `REMEDIATE_CLAIMERS` defaults `'0'`,
`REMEDIATION_DRY_RUN` defaults `'1'`, `REMEDIATION_WINDOW_WEEKS` defaults `'26'`.
So a normal cron run (no advanced_options) stays OFF — preserved.

**(c) Align operations.md Step 4** — rewrite the activation snippets (per the
documentation-maintenance rewrite-don't-append rule) to the REAL path:
```text
Trigger via the Actions "Run workflow" button, setting:
  advanced_options: remediate_claimers:1,remediation_dry_run:1,remediation_window_weeks:26
Review the dry-run summary, then re-run with remediation_dry_run:0 to execute.
```
Remove the misleading `# workflow_dispatch — dry-run pass` YAML-key snippets that
implied dedicated inputs.

**(d) Align environment.md** — in the REMEDIATE_CLAIMERS / REMEDIATION_DRY_RUN /
REMEDIATION_WINDOW_WEEKS sections, replace "Set ... via workflow_dispatch" with
"Set via the `advanced_options` workflow_dispatch field as
`remediate_claimers:1` (similarly `remediation_dry_run:0`,
`remediation_window_weeks:26`); the workflow's advanced_options parser exports
them to the environment. The Python defaults (OFF / dry-run / 26wk) apply when
unset, so a normal cron run never sweeps."
  </action>
  <verify>
    <automated>python -c "import yaml,sys; yaml.safe_load(open('.github/workflows/weekly-excel-generation.yml')); print('yaml ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "remediate_claimers)" .github/workflows/weekly-excel-generation.yml` >= 1 (parser case branch)
    - `grep -c "remediation_dry_run)" .github/workflows/weekly-excel-generation.yml` >= 1
    - `grep -c "remediation_window_weeks)" .github/workflows/weekly-excel-generation.yml` >= 1
    - The step `env:` block no longer pins `REMEDIATE_CLAIMERS: '0'` as a literal — verify `grep -c "REMEDIATE_CLAIMERS: '0'" .github/workflows/weekly-excel-generation.yml` == 0
    - No NEW top-level `workflow_dispatch` input named `remediate_claimers` was added (respect 10-input limit) — verify `remediate_claimers` does NOT appear under the `inputs:` block (only inside the parser `case` + the advanced_options description)
    - operations.md Step 4 contains `advanced_options: remediate_claimers:1` (or `remediate_claimers:1` within an advanced_options instruction) and no longer contains the bare `# workflow_dispatch — dry-run pass` snippet implying a dedicated input
    - environment.md REMEDIATE_CLAIMERS section references `advanced_options`
    - `python -c "import yaml; yaml.safe_load(open('.github/workflows/weekly-excel-generation.yml'))"` exits 0 (valid YAML)
  </acceptance_criteria>
  <done>The advanced_options parser activates remediation; the literal pins no longer block it; docs describe the real path; a normal cron run still defaults to OFF.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: WR-04 — protect _Unknown_Foreman in the isolated path; IN-02 counter clarity; IN-04 datetime cleanup</name>
  <read_first>
    - generate_weekly_pdfs.py run_claimer_remediation (the full function ~4009-4163) and _GARBAGE_PATTERNS (~4009)
    - tests/test_claimer_remediation.py (all classes: _BaseRemediationTest, TestDryRunNeverDeletes, TestExecuteDeletesOnlyGarbage, TestLiveIdentityExemption, TestIsolationPathValidWrWeeksNone, TestWindowFilter, TestUnparseableFilesIgnored — to add WR-04 + IN-02 tests and confirm no regression)
    - generate_weekly_pdfs.py main() dispatch (~7802-7818) — the isolated call passes valid_wr_weeks=None
  </read_first>
  <behavior>
    - Test 1 (WR-04, RED→GREEN): in EXECUTE mode with valid_wr_weeks=None, a `*_Unknown_Foreman*` attachment is NOT deleted (it is a legitimate current sentinel); a `*_NO_MATCH*` attachment IS deleted. (Drive the real run_claimer_remediation with a mocked smartsheet client exposing rows/attachments + delete_attachment; assert delete_attachment called for the NO_MATCH att id and NOT for the Unknown_Foreman att id.)
    - Test 2 (WR-04 with exemption present): when valid_wr_weeks IS provided (non-isolated path), behavior is unchanged from today — both garbage tokens are eligible, subject to the live-identity exemption (preserve TestLiveIdentityExemption / TestExecuteDeletesOnlyGarbage).
    - Test 3 (IN-02): out_of_window counts ONLY out-of-window GARBAGE files (or the metric is clearly named) — assert a clean out-of-window file does not inflate out_of_window. (Verify via the summary counters returned/logged.)
  </behavior>
  <action>
**(a) WR-04 — isolated-path _Unknown_Foreman protection.** `_NO_MATCH` is a pure
Smartsheet `#NO MATCH` error token (never a legitimate filename component), so it
is always garbage. `_Unknown_Foreman` is a LEGITIMATE current sentinel (emitted
when `Foreman Assigned?` is blank → `effective_user` = `'Unknown Foreman'`), so a
current `_Unknown_Foreman` file is a real billing artifact. In the isolated path
(`valid_wr_weeks is None`) there is no live-identity exemption to protect it, so an
EXECUTE sweep would delete a current correct file with a data-absent window until
the next cron.

Chosen mitigation (state in a code comment citing WR-04): split the garbage
classification by whether the live-identity exemption is available.
- Keep `_GARBAGE_PATTERNS = ('_NO_MATCH', '_Unknown_Foreman')` for the
  non-isolated path (valid_wr_weeks provided) — the live-identity exemption
  protects current files there, so both tokens stay eligible (no behavior change).
- Add a module constant for the always-safe-to-delete subset:
  `_ALWAYS_GARBAGE_PATTERNS: tuple[str, ...] = ('_NO_MATCH',)`.
- In `run_claimer_remediation`, compute the active pattern set per the path:
  ```python
  _patterns = _GARBAGE_PATTERNS if valid_wr_weeks is not None else _ALWAYS_GARBAGE_PATTERNS
  _is_garbage = any(pat in _name for pat in _patterns)
  ```
  So in the isolated path only `_NO_MATCH` is swept; `_Unknown_Foreman` is left
  alone (it self-heals after Sub-project E activation per runbook Step 4
  sequencing, or via the non-isolated exemption-protected path).
- Update the function docstring `valid_wr_weeks` note to describe this: "When
  None (isolated path), only the always-garbage `_NO_MATCH` token is swept;
  `_Unknown_Foreman` is preserved because it is a legitimate current sentinel and
  there is no live-identity set to protect it (WR-04)."

**(b) IN-02 — out_of_window counter clarity.** Move the garbage-pattern check
BEFORE the window filter so `_total_out_of_window` counts only out-of-window
*garbage* files (the operator's blast-radius mental model). Concretely, reorder
within the per-attachment loop: parse identity → garbage-pattern check (continue
if clean) → window filter (`_total_out_of_window += 1; continue` if too old) →
live-identity exemption → dry-run/execute. This makes `out_of_window` mean
"garbage files skipped because they're older than the window" — unambiguous in the
summary line. (Alternative acceptable form: keep order but rename the metric /
add a one-line comment clarifying it counts ALL out-of-window attachments — prefer
the reorder so the count is genuinely garbage-only.)

**(c) IN-04 — drop the shadowing local import.** Remove `import datetime as _dt`
from inside `run_claimer_remediation` and replace `_dt.date.today()`,
`_dt.timedelta(...)`, `_dt.datetime.strptime(...)` with the module-level
`datetime.date.today()`, `datetime.timedelta(...)`, `datetime.datetime.strptime(...)`.
Confirm the module-level `datetime` import is present (it is, used throughout).

Additive/surgical: no change to the dispatch site, the dry-run/execute branching,
the live-identity exemption logic, or the summary-line format (the format change
for IN-03 is a DOC change, not a code change — see Task 3).

Add the three behavior tests to tests/test_claimer_remediation.py. Name them e.g.
`test_isolated_path_preserves_unknown_foreman`,
`test_isolated_path_deletes_no_match`,
`test_out_of_window_counts_only_garbage` (and keep
TestLiveIdentityExemption / TestExecuteDeletesOnlyGarbage passing — for those
non-isolated tests, valid_wr_weeks is provided so both tokens remain eligible).
  </action>
  <verify>
    <automated>python -m pytest tests/test_claimer_remediation.py -q && python -m py_compile generate_weekly_pdfs.py</automated>
  </verify>
  <acceptance_criteria>
    - generate_weekly_pdfs.py defines `_ALWAYS_GARBAGE_PATTERNS` containing `'_NO_MATCH'` and NOT `'_Unknown_Foreman'` — verify `grep -c "_ALWAYS_GARBAGE_PATTERNS" generate_weekly_pdfs.py` >= 2
    - run_claimer_remediation selects the pattern set by `valid_wr_weeks is not None` — verify the function references `_ALWAYS_GARBAGE_PATTERNS` in the branch where the exemption set is absent
    - generate_weekly_pdfs.py run_claimer_remediation no longer contains `import datetime as _dt` — verify `grep -c "import datetime as _dt" generate_weekly_pdfs.py` == 0
    - run_claimer_remediation references `datetime.date.today()` (module-level) — verify `grep -c "datetime.date.today()" generate_weekly_pdfs.py` >= 1
    - tests/test_claimer_remediation.py asserts an EXECUTE sweep with valid_wr_weeks=None does NOT delete an `_Unknown_Foreman` attachment but DOES delete a `_NO_MATCH` attachment
    - tests/test_claimer_remediation.py asserts out_of_window does not include a clean (non-garbage) out-of-window file (IN-02)
    - `python -m pytest tests/test_claimer_remediation.py -q` exits 0
    - `python -m py_compile generate_weekly_pdfs.py` exits 0
  </acceptance_criteria>
  <done>Isolated EXECUTE sweep deletes only _NO_MATCH (preserves _Unknown_Foreman); non-isolated path unchanged; out_of_window counts only garbage; local datetime alias removed.</done>
</task>

<task type="auto">
  <name>Task 3: IN-03 — align operations.md dry-run quote to the actual summary line</name>
  <read_first>
    - generate_weekly_pdfs.py run_claimer_remediation summary log (the `✅ run_claimer_remediation [{_mode}] complete: scanned=... garbage=... deleted=... exempted=... out_of_window=...` line ~4156-4163) — the AUTHORITATIVE format
    - website/docs/runbook/operations.md Step 4 (~133-158) — the misquoted `would delete N garbage attachments across TARGET/PPP, 0 executed` line (~144)
  </read_first>
  <action>
In website/docs/runbook/operations.md Step 4, REWRITE (per the
documentation-maintenance rewrite-don't-append rule) the dry-run review paragraph
so the quoted summary line matches what the code actually logs. Replace the
phrase `would delete N garbage attachments across TARGET/PPP, 0 executed` with the
real summary-line format:
```
✅ run_claimer_remediation [DRY-RUN] complete: scanned=N garbage=N deleted=0 exempted=N out_of_window=N
```
Also mention the per-attachment dry-run lines an operator can grep:
`🔍 [DRY-RUN] would delete garbage attachment att=... sheet=... wr=... week=... variant=...`.
Keep the surrounding instructional flow (run dry-run first, review counts, re-run
with execute). Do NOT change the code log strings (they are correct); only the
doc is wrong.
  </action>
  <verify>
    <automated>cd website && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - operations.md contains the literal `run_claimer_remediation [DRY-RUN] complete` (matching the code summary)
    - operations.md no longer contains the string `would delete N garbage attachments across TARGET/PPP, 0 executed`
    - operations.md references the per-attachment dry-run line phrase `would delete garbage attachment`
    - `cd website && npm run build` exits 0 (no broken doc / cross-link)
  </acceptance_criteria>
  <done>operations.md Step 4 quotes the real run_claimer_remediation summary-line format; the website builds.</done>
</task>

<task type="auto">
  <name>Task 4: Living Ledger entry (date-stamped) summarizing the gap-closure round + full-suite green gate</name>
  <read_first>
    - CLAUDE.md — the "## Living Ledger (Auto-Updated Context)" section and the most recent entry `[2026-05-26 14:55]` (Phase 2) to match the established narrative style and the AUTONOMOUS CLOUD MEMORY INJECTION rule
    - .planning/phases/02-attribution-bulk-prefetch-historical-claimer-remediation/02-05-PLAN.md and this plan — to summarize all 10 findings closed
  </read_first>
  <action>
Append ONE new Living Ledger entry to the bottom of CLAUDE.md's Living Ledger
section, prepended with a `[YYYY-MM-DD HH:MM]` timestamp (executor sets the
actual time at commit). The entry MUST be a synthesized narrative (per the
documentation-maintenance changelog rule — not a commit-message dump) covering:
- This is the Phase 2 gap-closure round closing the 02-REVIEW.md findings
  (1 BLOCKER + 5 WARNING + 4 INFO = 10 total).
- CR-01: the deployment-ordering hazard — a missing `lookup_attribution_bulk` RPC
  (PGRST202) previously HELD all B/C/sub-helper billing files. Fix:
  `prefetch_attribution` now returns a distinct `rpc_missing` status (vs transient
  `fetch_failure`), and a default-ON workflow-pinned `ATTRIBUTION_BULK_PREFETCH_FALLBACK`
  degrades `rpc_missing` to the deployed per-row `lookup_attribution` path
  (correctness-preserving — same frozen data, slower — NOT a D-04 violation),
  while a genuine transient outage still HOLDs (D-04 preserved, no 137k-storm
  regression). The merge no longer depends on deploy ordering.
- WR-01: the WR-sanitization split-brain — `resolve_claimer`'s prefetched-map
  lookup key is now sanitized with the same `_WR_SANITIZE` expression as the map
  key (CLAUDE.md [2026-04-23 18:25] consumer-consistency rule).
- WR-02: the documented remediation activation path now works — driven via the
  `advanced_options` parser (`remediate_claimers:1,remediation_dry_run:0,...`)
  rather than non-existent dedicated inputs (respecting the 10-input limit); the
  literal env pins were removed so the parser/$GITHUB_ENV path wins; Python
  defaults keep a normal cron run OFF.
- WR-03: corrected the misleading D-consumer comment (no `action='disabled'`).
- WR-04: the isolated EXECUTE sweep now only deletes the always-garbage `_NO_MATCH`
  token; `_Unknown_Foreman` is preserved (it is a legitimate current sentinel) —
  closing a data-loss window; the non-isolated exemption-protected path is
  unchanged.
- WR-05: the sub-helper outage path now emits its per-WR fetch_failure WARNING
  again (observability restored).
- IN-01/IN-02/IN-03/IN-04: dead imports removed; out_of_window counts only
  garbage; runbook dry-run quote aligned; local `import datetime as _dt` dropped.
- Record at least 2-3 durable NEW RULES, e.g.:
  (1) a hard-runtime RPC dependency MUST distinguish "not deployed" (permanent,
  degrade gracefully) from "transient outage" (preserve the strict policy), and
  the degrade path MUST be a default-ON workflow-pinned kill switch so deploy
  ordering can never suppress billing;
  (2) an attachment-deleting sweep with no live-identity set MUST restrict its
  garbage set to tokens that are NEVER a legitimate filename component
  (`_NO_MATCH`), never an ambiguous sentinel (`_Unknown_Foreman`);
  (3) a rarely-used destructive operator control is wired through the
  `advanced_options` parser, never a new top-level input (10-input limit), and a
  literal step-`env:` pin will silently mask the parser — remove the literal so
  $GITHUB_ENV wins.
- End with the final `pytest tests/` count.

Then run the full suite and confirm green with a count strictly greater than the
02-05 close. Fix any regression in the code under change (do not weaken tests).
  </action>
  <verify>
    <automated>python -m pytest tests/ -q</automated>
  </verify>
  <acceptance_criteria>
    - CLAUDE.md Living Ledger has a NEW entry with a `[20` `:` timestamp prefix (matches `^- \[20..-..-.. ..:..\]`) added after the `[2026-05-26 14:55]` Phase 2 entry
    - The new entry contains the strings `ATTRIBUTION_BULK_PREFETCH_FALLBACK`, `rpc_missing`, `_Unknown_Foreman`, and `advanced_options`
    - The new entry ends with a `pytest tests/` count line (e.g. `passed`)
    - `python -m pytest tests/ -q` exits 0 with passed count strictly greater than the 02-05 close
    - `python -m py_compile generate_weekly_pdfs.py` exits 0
  </acceptance_criteria>
  <done>A synthesized, date-stamped Living Ledger entry for the whole gap-closure round is in CLAUDE.md; pytest tests/ green with a growing count.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator → workflow_dispatch → remediation | A destructive (attachment-deleting) operation activated by an operator-supplied advanced_options string |
| run_claimer_remediation → Smartsheet attachments | The garbage-token + window + exemption gates decide which attachments are permanently deleted |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-06-01 | Tampering (accidental data loss) | isolated EXECUTE sweep deleting a valid _Unknown_Foreman file | mitigate | isolated path restricts deletion to the always-garbage `_NO_MATCH` token; `_Unknown_Foreman` preserved (WR-04); regression test asserts it is not deleted |
| T-02-06-02 | Elevation / unintended activation | operator believes a dry-run ran when nothing executed (unreachable documented path) | mitigate | wire the real advanced_options parser path + remove the masking literal env pins; default stays OFF + dry-run-first (WR-02) |
| T-02-06-03 | Repudiation (misleading summary) | out_of_window count conflates clean+garbage, misinforming blast-radius reasoning | mitigate | reorder so out_of_window counts only garbage (IN-02); runbook quotes the real summary (IN-03) |
| T-02-06-04 | Denial of Service (unbounded sweep) | window filter mis-set to 0 sweeping all history | accept | REMEDIATION_WINDOW_WEEKS default 26; dry-run-first + default-OFF gate are the existing controls; documented in environment.md |
| T-02-06-05 | Tampering (cron auto-fire) | remediation firing on a scheduled run | mitigate | REMEDIATE_CLAIMERS Python default '0'; no advanced_options on cron → OFF; verified by the existing default-OFF tests + Task 1 acceptance |
</threat_model>

<verification>
- `python -m pytest tests/ -q` exits 0; passed count > 02-05 close.
- `python -m py_compile generate_weekly_pdfs.py` exits 0.
- `python -c "import yaml; yaml.safe_load(open('.github/workflows/weekly-excel-generation.yml'))"` exits 0.
- `cd website && npm run build` exits 0.
- grep gates in each task's acceptance_criteria hold.
- WR-02/WR-04/IN-02/IN-03/IN-04 closed per their checks; Living Ledger entry present.
</verification>

<success_criteria>
- Remediation is operator-reachable via advanced_options and observable.
- The isolated EXECUTE sweep cannot delete a valid _Unknown_Foreman file.
- out_of_window is unambiguous; the runbook quote matches the code.
- The shadowing datetime alias is gone.
- The date-stamped Living Ledger entry is committed in the same change set.
- pytest count strictly grows; website builds.
</success_criteria>

<output>
After completion, create
`.planning/phases/02-attribution-bulk-prefetch-historical-claimer-remediation/02-06-SUMMARY.md`
</output>
