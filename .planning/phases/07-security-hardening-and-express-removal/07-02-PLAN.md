---
phase: 07-security-hardening-and-express-removal
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/security-probe.ts
  - scripts/package.json
autonomous: false
requirements: [SEC-01, SEC-05]
user_setup:
  - service: supabase
    why: "The probe signs in as a dedicated pending-role account using ONLY the anon key (never service_role) — the account must exist with role='pending' (D-08)"
    env_vars:
      - name: SUPABASE_URL
        source: "Supabase Dashboard → Project poeyztlmsawfoqlanucc → Settings → API → Project URL (same value as VITE_SUPABASE_URL)"
      - name: SUPABASE_ANON_KEY
        source: "Supabase Dashboard → Project poeyztlmsawfoqlanucc → Settings → API → anon public key (safe to expose; RLS is the guard per SEC-03)"
      - name: SUPABASE_PROBE_PENDING_EMAIL
        source: "Email of the dedicated pending-role test account (e.g. security-probe@linetec-test.internal)"
      - name: SUPABASE_PROBE_PENDING_PASSWORD
        source: "Password for the pending-role test account; store in GitHub Actions Secrets for CI runs"
    dashboard_config:
      - task: "Create a dedicated test account in Supabase Auth (project poeyztlmsawfoqlanucc); confirm its profiles.role = 'pending' (NOT admin/billing) via SELECT role FROM profiles WHERE email = '<probe-email>' — RESEARCH Pitfall 5"
        location: "Supabase Dashboard → Authentication → Users (+ Table Editor → profiles)"
      - task: "Add SUPABASE_ANON_KEY, SUPABASE_PROBE_PENDING_EMAIL, SUPABASE_PROBE_PENDING_PASSWORD to GitHub Actions Secrets. Do NOT add SUPABASE_SERVICE_ROLE_KEY to the probe's step env."
        location: "GitHub repo → Settings → Secrets and variables → Actions"

must_haves:
  truths:
    - "An anonymous REST SELECT on /rest/v1/artifacts returns an empty array against the live project"
    - "An anonymous Storage GET on the excel-artifacts bucket returns 400/403 (access denied)"
    - "A pending-role JWT receives zero artifact rows AND cannot generate a signed URL"
    - "The probe uses ONLY the public anon key + pending-role sign-in — service_role never appears in the probe path"
    - "Signed download URLs are confirmed 5-minute TTL and single-object scoped (SEC-05) — audit only, no code change"
    - "D-07: the probe is an automated, re-runnable harness asserting each live success-criterion outcome (regression-protects across future RLS changes), plus a one-time manual sign-off recorded in 07-SECURITY.md"
    - "D-08: the probe is a standalone scripts/ Node/TS script using ONLY the public anon key + a dedicated pending-role test account — never service_role — runnable locally or in CI against poeyztlmsawfoqlanucc"
  artifacts:
    - path: "scripts/security-probe.ts"
      provides: "Re-runnable live RLS/signed-URL verification harness (SEC-01, SEC-05)"
      contains: "createSignedUrl"
      min_lines: 60
    - path: "scripts/package.json"
      provides: "tsx/supabase-js dependency declaration so the probe runs via npx tsx in CI"
  key_links:
    - from: "scripts/security-probe.ts"
      to: "https://poeyztlmsawfoqlanucc.supabase.co/rest/v1/artifacts (anon + pending JWT)"
      via: "createClient(SUPABASE_URL, SUPABASE_ANON_KEY) + signInWithPassword"
      pattern: "createClient\\(SUPABASE_URL, SUPABASE_ANON_KEY\\)"
    - from: "scripts/security-probe.ts"
      to: "Supabase Storage excel-artifacts bucket"
      via: "createSignedUrl on pending JWT (must be denied)"
      pattern: "createSignedUrl"
---

<objective>
Create a standalone, re-runnable Node/TypeScript security probe in `scripts/` that
asserts the live RLS + signed-URL guarantees (SEC-01, SEC-05) against the live
Supabase project `poeyztlmsawfoqlanucc`, using ONLY the public anon key + a
dedicated pending-role test account (never `service_role`, per D-08). Audit-confirm
that `useDownloadArtifact.ts` already satisfies SEC-05 (5-min TTL, single-object).

Purpose: Regression-protect the data-access guarantees the whole portal depends on.
The probe proves, against the live deployment, that anon and pending-role principals
cannot read billing artifact metadata or generate signed download URLs — closing
SEC-01. SEC-05 (5-min single-object signed URLs) is already implemented; this plan
audits and confirms it, not changes it.

Output: `scripts/security-probe.ts` (idempotent, exit-code contract), a
`scripts/package.json` declaring its deps, a confirmed local probe run with all
assertions passing, and a grep-confirmed SEC-05 audit note.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-security-hardening-and-express-removal/07-CONTEXT.md
@.planning/phases/07-security-hardening-and-express-removal/07-RESEARCH.md
@.planning/phases/07-security-hardening-and-express-removal/07-PATTERNS.md
@scripts/publish_artifacts_to_supabase.py
@portal-v2/src/lib/supabase.ts
@portal-v2/src/hooks/useDownloadArtifact.ts

<interfaces>
<!-- The probe's analog (scripts/publish_artifacts_to_supabase.py) is a Python script;
     transpose its conventions to TS: header docstring, env-var contract up front,
     explicit process.exit(1) on failure, loud-not-silent errors. RESEARCH §Question 5
     and 07-PATTERNS.md give the COMPLETE probe shape — copy it. -->

SEC-05 (audit-only) — current portal-v2/src/hooks/useDownloadArtifact.ts:
```typescript
const BUCKET = 'excel-artifacts';
const SIGNED_URL_TTL = 300; // 5 min, single-object (Phase 03 D-10 / DATA-05 / SEC-05)
// ...
  .from(BUCKET)
  .createSignedUrl(storagePath, SIGNED_URL_TTL, { download: filename });
```
=> SEC-05 ALREADY SATISFIED (TTL=300, single storagePath, { download } scope). No code change.

Supabase client pattern (portal-v2/src/lib/supabase.ts): createClient(url, anonKey) — anon key only.
Live project ref: poeyztlmsawfoqlanucc (2,383 artifacts rows; CI publish live).
Bucket: excel-artifacts (public:false).

SENTRY-WRAP EXEMPTION (pre-empts a spurious Living-Ledger entry): CLAUDE.md's
"wrap new optimizations in Sentry" rule targets PRODUCTION Smartsheet upload scripts
(the deployed billing pipeline). The probe is a ONE-SHOT diagnostic, not a deployed
service — it is EXEMPT from the Sentry telemetry requirement. It already honors the
"never swallow exceptions" rule via the top-level `.catch((err) => { console.error(err);
process.exit(1); })` + non-zero exit-code contract. State this exemption in the
probe's JSDoc header (Task 1, step 1) so a future reader does not add Sentry wiring
or file a ledger entry for a missing wrap.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Create scripts/security-probe.ts + scripts/package.json (the live RLS/signed-URL harness)</name>
  <files>scripts/security-probe.ts, scripts/package.json</files>
  <read_first>
    - scripts/publish_artifacts_to_supabase.py (the structural analog — env-var guard, exit-code contract, loud errors; lines 1-90)
    - .planning/phases/07-security-hardening-and-express-removal/07-PATTERNS.md §"scripts/security-probe.ts" (the COMPLETE probe shape to transpose — docstring, env guard, core probe, exit-code tail)
    - .planning/phases/07-security-hardening-and-express-removal/07-RESEARCH.md §"Question 5" (probe structure + run commands) and §"Common Pitfalls" Pitfall 5 (pending-account role guard)
    - portal-v2/src/lib/supabase.ts (the createClient(url, anonKey) pattern — anon key only, NEVER service_role)
  </read_first>
  <action>
    Create `scripts/security-probe.ts` as the FIRST TypeScript entry in `scripts/`
    (all existing scripts are Python — transpose their conventions). Copy the
    complete probe shape verbatim from 07-PATTERNS.md §"scripts/security-probe.ts"
    (it reproduces RESEARCH §Question 5). The script MUST:

    1. Open with a JSDoc header documenting the 4 consumed env vars
       (SUPABASE_URL, SUPABASE_ANON_KEY, SUPABASE_PROBE_PENDING_EMAIL,
       SUPABASE_PROBE_PENDING_PASSWORD), the local + CI run commands, and the
       security posture note: "service_role is NEVER referenced — SEC-03 hard
       constraint; failure logs contain only assertion IDs + HTTP status codes,
       never row data." ALSO include a DIAGNOSTIC-ONLY exemption note, e.g.:
       ```typescript
       /**
        * Diagnostic-only tool — NOT a deployed/production service. This one-shot
        * security probe is EXEMPT from the CLAUDE.md "wrap new optimizations in
        * Sentry" rule (that rule targets the production Smartsheet upload pipeline).
        * It already satisfies the never-swallow-exceptions rule via the top-level
        * .catch() + non-zero process.exit(1) exit-code contract below — no Sentry
        * wiring required. Do not file a Living-Ledger entry for a missing wrap.
        */
       ```
       This pre-empts a spurious Living-Ledger entry for a "missing Sentry wrap".

    2. Guard all 4 env vars up front; `process.exit(1)` with a clear message if any
       is missing (mirror publish_artifacts_to_supabase.py's required-env check).

    3. Instantiate `const anonClient = createClient(SUPABASE_URL, SUPABASE_ANON_KEY)`
       and a separate `pendingClient = createClient(SUPABASE_URL, SUPABASE_ANON_KEY)`.
       NEVER pass a service_role key to createClient — the probe operates under the
       same RLS constraints as the real portal.

    4. Run these 4 assertions, incrementing a `failures` counter and logging
       PASS/FAIL with the SEC ID (NEVER logging row data):
       - SEC-01a: `anonClient.from('artifacts').select('id').limit(1)` → expect empty.
         FAIL if `anonRows && anonRows.length > 0`.
       - SEC-01b: `await fetch(\`${SUPABASE_URL}/storage/v1/object/excel-artifacts/test\`)`
         → expect status 400 or 403. FAIL otherwise.
       - Sign in: `pendingClient.auth.signInWithPassword({ email: PROBE_EMAIL, password: PROBE_PASSWORD })`;
         `process.exit(1)` if signInError (the account must exist).
       - SEC-01c: `pendingClient.from('artifacts').select('id').limit(1)` → expect 0 rows.
         FAIL if `pendingRows && pendingRows.length > 0`.
       - SEC-05/SEC-01d: `pendingClient.storage.from('excel-artifacts').createSignedUrl('any-path.xlsx', 300)`
         → expect denied. FAIL if `signedData?.signedUrl` is truthy.

    5. End with: if `failures > 0` log `SECURITY PROBE FAILED: N assertion(s) failed`
       and `process.exit(1)`; else log `All security probe assertions passed.` and
       exit 0 implicitly. Tail with (this is the never-swallow guarantee that makes
       the Sentry-wrap exemption above safe):
       `runProbe().catch((err: unknown) => { console.error(err); process.exit(1); });`

    Also create `scripts/package.json` declaring the probe's runtime deps so CI can
    run it via `npx tsx`:
    ```json
    {
      "name": "gsd-scripts",
      "private": true,
      "type": "module",
      "description": "Standalone repo utility scripts (security probe, etc.)",
      "dependencies": {
        "@supabase/supabase-js": "^2.45.4"
      },
      "devDependencies": {
        "tsx": "^4.19.0"
      }
    }
    ```
    (Use `@supabase/supabase-js@^2.45.4` to match portal-v2's installed version per
    RESEARCH Standard Stack. Alternatively the probe may be run with `npx tsx`
    resolving portal-v2's node_modules — but declaring deps here makes the probe
    self-contained for CI.)

    DO NOT modify useDownloadArtifact.ts — SEC-05 is already satisfied (audit-only).
  </action>
  <verify>
    <automated>cd "scripts" && node -e "const fs=require('fs');const s=fs.readFileSync('security-probe.ts','utf8'); const fail=[]; if(/SERVICE_ROLE|service_role/.test(s))fail.push('service_role referenced in probe'); for(const t of ['createClient(SUPABASE_URL, SUPABASE_ANON_KEY)','signInWithPassword','createSignedUrl','SEC-01a','SEC-01b','SEC-01c','SEC-05','process.exit(1)']){ if(!s.includes(t))fail.push('missing: '+t);} if(fail.length){console.error(fail.join('\n'));process.exit(1)} console.log('PASS: probe shape correct, no service_role')"</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/security-probe.ts` exists and is ≥ 60 lines.
    - `grep -ci "service_role\|SERVICE_ROLE" scripts/security-probe.ts` returns 0 (probe never references service_role — SEC-03/D-08 hard constraint).
    - The file contains the literal `createClient(SUPABASE_URL, SUPABASE_ANON_KEY)`, `signInWithPassword`, `createSignedUrl`, and the markers `SEC-01a`, `SEC-01b`, `SEC-01c`, `SEC-05`.
    - The file contains `process.exit(1)` (exit-code contract) and a guard rejecting missing env vars.
    - The JSDoc header contains the diagnostic-only / Sentry-wrap-exemption note (so no spurious Living-Ledger entry is filed for a missing Sentry wrap).
    - `scripts/package.json` exists and declares `@supabase/supabase-js` and `tsx`.
    - `cd scripts && npx tsc --noEmit security-probe.ts --moduleResolution node --module esnext --target es2022 --skipLibCheck` exits 0 (TypeScript is valid) — OR the equivalent type-check passes if a tsconfig is added.
  </acceptance_criteria>
  <done>scripts/security-probe.ts is a syntactically valid, type-checking TS probe with all 4 assertions, the exit-code contract, the diagnostic-only Sentry-exemption JSDoc note, and zero service_role references; scripts/package.json declares its deps.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: [BLOCKING] Run the probe against the LIVE deployment — all assertions pass (SEC-01) + SEC-05 audit note</name>
  <files>scripts/security-probe.ts (executed, not modified); portal-v2/src/hooks/useDownloadArtifact.ts (audit-only, not modified)</files>
  <read_first>
    - .planning/phases/07-security-hardening-and-express-removal/07-RESEARCH.md §"Question 5" (Running locally / Running in CI) and Pitfall 5 (verify probe account role = pending)
    - .planning/phases/07-security-hardening-and-express-removal/07-CONTEXT.md §D-07, §D-08
    - portal-v2/src/hooks/useDownloadArtifact.ts (SEC-05 audit target — confirm TTL=300 + single object)
  </read_first>
  <action>
    Verify the probe account role = 'pending', then run scripts/security-probe.ts
    against the LIVE project poeyztlmsawfoqlanucc using the env vars from user_setup,
    and confirm all four assertions PASS with exit 0. Then perform the SEC-05
    audit-only grep (no code change). Record the full probe output + the SEC-05 grep
    result for 07-04's 07-SECURITY.md "Probe Results (D-07/D-08)" section. This is the
    one-time manual sign-off required by D-07. Exact steps are in how-to-verify.
  </action>
  <what-built>
    Task 1 created scripts/security-probe.ts. It is idempotent (writes no state) and
    asserts, against the live project poeyztlmsawfoqlanucc, that anon and pending-role
    principals cannot read artifacts or generate signed URLs. This BLOCKING checkpoint
    runs it for real and records the outcome.
  </what-built>
  <how-to-verify>
    PREREQUISITE (RESEARCH Pitfall 5): In the Supabase Table Editor, confirm the probe
    account's role is 'pending':
      `SELECT role FROM profiles WHERE email = '<SUPABASE_PROBE_PENDING_EMAIL>';`
    It MUST return `pending`. If it returns admin/billing the "0 rows" assertion will
    false-fail — fix the role first.

    Then run the probe against the live project (from the repo root):
    ```bash
    SUPABASE_URL=https://poeyztlmsawfoqlanucc.supabase.co \
    SUPABASE_ANON_KEY=<anon-key> \
    SUPABASE_PROBE_PENDING_EMAIL=<probe-email> \
    SUPABASE_PROBE_PENDING_PASSWORD=<probe-password> \
    npx tsx scripts/security-probe.ts ; echo "EXIT:$?"
    ```
    Confirm:
    - The output logs `PASS SEC-01a`, `PASS SEC-01b`, `PASS SEC-01c`, `PASS SEC-05/SEC-01d`.
    - `EXIT:0`.
    - NO row data (work_request, filename, etc.) appears anywhere in the output —
      only assertion IDs + HTTP status codes.

    SEC-05 audit (no code change): confirm
      `grep -n "SIGNED_URL_TTL" portal-v2/src/hooks/useDownloadArtifact.ts`
    shows `const SIGNED_URL_TTL = 300;` and that `createSignedUrl(storagePath, SIGNED_URL_TTL, ...)`
    passes a single object path (not a wildcard). Record this as "SEC-05 confirmed —
    no change" for 07-04's 07-SECURITY.md.

    Record the full probe output + the SEC-05 grep result for the eventual
    07-SECURITY.md "Probe Results (D-07/D-08)" section (written in 07-04). This is the
    one-time manual sign-off required by D-07.
  </how-to-verify>
  <verify>
    <automated>echo "Manual BLOCKING checkpoint: run 'npx tsx scripts/security-probe.ts' with live env vars; expect EXIT:0 + 4 PASS lines. SEC-05 audit: grep -n SIGNED_URL_TTL portal-v2/src/hooks/useDownloadArtifact.ts (== 300)."</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsx scripts/security-probe.ts` exits 0 with all four PASS lines (SEC-01a, SEC-01b, SEC-01c, SEC-05/SEC-01d) against the live poeyztlmsawfoqlanucc project.
    - The probe output contains no artifact row data (only assertion IDs + HTTP status codes).
    - `grep -n "SIGNED_URL_TTL" portal-v2/src/hooks/useDownloadArtifact.ts` shows `= 300` and createSignedUrl takes a single storagePath — recorded as SEC-05 confirmed, no change.
    - The probe account's `profiles.role` was verified = 'pending' before the run.
    - The full probe output + SEC-05 audit note are captured for 07-04's 07-SECURITY.md.
  </acceptance_criteria>
  <done>The live probe exits 0 with all 4 assertions PASS, SEC-05 is audit-confirmed (TTL=300, single object), and both are recorded for 07-04's 07-SECURITY.md.</done>
  <resume-signal>Type "approved" once the live probe exits 0 with all 4 assertions PASS and the SEC-05 audit is recorded, or paste the failing assertion output.</resume-signal>
</task>

</tasks>

<threat_model>
ASVS Level: L1. Block on: HIGH severity. Primary ASVS focus: V4 Access Control.

## Trust Boundaries

| Boundary | Description | Data Crossing |
|----------|-------------|---------------|
| anonymous/public → PostgREST `/rest/v1/artifacts` | Anon key is public by design; DB RLS is the only guard. | Billing artifact metadata (PII-sensitive) |
| authenticated `pending` → artifacts + Storage | A logged-in but unapproved user must get zero rows AND no signed URL. | Billing metadata + Excel files |
| probe script → live Supabase | The probe must operate under the SAME RLS constraints as the real portal — anon key + pending sign-in only, never service_role. | anon key + pending JWT |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-03-anon-read | Information Disclosure | `public.artifacts` SELECT | mitigate | RLS `artifacts_select_billing_or_admin` (verified in portal_schema.sql); probe SEC-01a asserts anon → [] against the live project. |
| T-07-03-pending-read | Elevation of Privilege | `public.artifacts` SELECT | mitigate | Same RLS; probe SEC-01c asserts pending JWT → 0 rows. Pitfall 5 guard (account role = pending) prevents false negatives. |
| T-07-03-anon-storage | Information Disclosure | `storage.objects` excel-artifacts | mitigate | Bucket `public:false` + Storage SELECT policy; probe SEC-01b asserts anon GET → 400/403. |
| T-07-03-pending-signurl | Information Disclosure | `createSignedUrl` | mitigate | Storage SELECT policy blocks pending; probe SEC-05/SEC-01d asserts createSignedUrl → denied for pending JWT. |
| T-07-03-signurl-overexposure | Information Disclosure | Signed download URL TTL/scope | mitigate | `SIGNED_URL_TTL = 300` (5 min) + single `storagePath` + `{ download }` scope in useDownloadArtifact.ts (audit-confirmed, no change) — no wildcard paths. SEC-05. |
| T-07-03-probe-leaks-rows | Information Disclosure | probe stdout / CI logs | mitigate | Probe logs ONLY assertion IDs + HTTP status codes, never row data (verified by Task 1 grep + Task 2 output review). |
| T-07-03-probe-service-role | Elevation of Privilege | probe credentials | mitigate | Probe uses anon key + signInWithPassword only; Task 1 grep gate `service_role → 0`. service_role never reaches the probe step env (honors SEC-03). |
</threat_model>

<verification>
- Task 1 `node -e` gate: probe shape correct, all assertion markers present, zero service_role references.
- Task 1: `npx tsc --noEmit` type-check passes; JSDoc header carries the diagnostic-only Sentry-exemption note.
- Task 2 (BLOCKING): live `npx tsx scripts/security-probe.ts` exits 0 with all 4 PASS lines; no row data in output.
- SEC-05 audit: `grep -n "SIGNED_URL_TTL" portal-v2/src/hooks/useDownloadArtifact.ts` → `= 300`, single object — recorded, no change.
</verification>

<success_criteria>
- scripts/security-probe.ts exists, type-checks, references no service_role, carries the diagnostic-only Sentry-exemption note, and asserts SEC-01a/b/c + SEC-05/SEC-01d.
- The probe runs green against the live project (anon → []; anon Storage → 403; pending → 0 rows; pending createSignedUrl → denied).
- SEC-05 is audit-confirmed (TTL=300, single-object) with no code change.
- All probe output + audit notes are captured for 07-04's 07-SECURITY.md.
</success_criteria>

<output>
After completion, create `.planning/phases/07-security-hardening-and-express-removal/07-02-SUMMARY.md` recording: the probe assertions + exit code from the live run, the SEC-05 audit confirmation, and the pending-account provisioning details — all to feed 07-04's 07-SECURITY.md "Probe Results (D-07/D-08)" section.
</output>
