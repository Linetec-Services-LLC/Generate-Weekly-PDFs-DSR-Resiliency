---
phase: 07-security-hardening-and-express-removal
plan: 03
type: execute
wave: 2
depends_on: [01, 02]
files_modified:
  - portal-v2/src/lib/mockData.ts
  - portal-v2/src/lib/api.ts
  - portal-v2/src/hooks/useRuns.ts
  - portal-v2/src/components/dashboard/CommandPalette.tsx
  - portal-v2/src/components/dashboard/ArtifactExplorer.tsx
  - portal-v2/src/components/dashboard/ArtifactPanel.tsx
  - portal-v2/src/components/dashboard/FilePreview.tsx
  - portal-v2/src/components/dashboard/InteractiveExcelView.tsx
  - portal-v2/src/components/dashboard/StyledExcelView.tsx
  - portal-v2/vite.config.ts
  - portal-v2/.env.example
  - portal-v2/vercel.json
  - portal/
autonomous: false
requirements: [SEC-03, SEC-02]
user_setup:
  - service: vercel
    why: "VITE_API_BASE_URL must be removed from the Vercel project env vars (Preview + Production) — Claude cannot edit Vercel dashboard env vars (D-01 / SEC-03)"
    dashboard_config:
      - task: "Delete VITE_API_BASE_URL from the Vercel project Environment Variables (both Preview and Production)"
        location: "Vercel Dashboard → Project → Settings → Environment Variables"
  - service: render
    why: "If any Render/Railway service still points at portal/ as its root, deleting the directory would 404 it on next deploy (RESEARCH A4 / Open Questions §2). Pre-removal acceptance item — confirm BEFORE the git rm."
    dashboard_config:
      - task: "Confirm no active Render OR Railway (or any other) service uses this repo's portal/ as its root directory before the git rm; decommission it if found"
        location: "Render Dashboard / Railway Dashboard → Services"

must_haves:
  truths:
    - "grep -r SERVICE_ROLE in portal-v2/src returns nothing (SEC-03)"
    - "VITE_API_BASE_URL, fetch('/api', API_BASE, and lib/api imports are absent from portal-v2/src (D-02 grep-gate)"
    - "The portal/ Express backend directory is deleted from git (D-01)"
    - "The vercel.json SPA rewrite is intact and the live deep-link /dashboard does not 404 after removal (D-02)"
    - "USE_MOCK no longer depends on VITE_API_BASE_URL — it is gated solely on the explicit VITE_USE_MOCK flag (prevents the silent sample-data bug)"
    - "The enforcing Content-Security-Policy header replaces Content-Security-Policy-Report-Only ONLY after the 07-01 Task 2 zero-violation walkthrough passed (SEC-02 enforce-flip)"
  artifacts:
    - path: "portal-v2/src/lib/mockData.ts"
      provides: "USE_MOCK gated only on VITE_USE_MOCK (no VITE_API_BASE_URL dependency)"
      contains: "VITE_USE_MOCK"
    - path: "portal-v2/vercel.json"
      provides: "Enforcing Content-Security-Policy (flipped from Report-Only)"
      contains: "Content-Security-Policy"
  key_links:
    - from: "portal-v2/ (entire source tree)"
      to: "removed Express backend"
      via: "zero residual VITE_API_BASE_URL / fetch('/api' / lib/api imports"
      pattern: "VITE_API_BASE_URL"
    - from: "portal-v2/vercel.json rewrites"
      to: "/index.html"
      via: "SPA catch-all (must survive removal intact)"
      pattern: "\"destination\": \"/index.html\""
---

<objective>
Permanently remove the legacy Express backend (`portal/`) — but ONLY in the
mandatory order: (a) sever every `portal-v2/src` coupling to the Express surface,
(b) run the BLOCKING D-02 grep-gate + SEC-03 secret gate, (c) `git rm -r portal/`,
(d) flip the CSP from Report-Only to enforcing (gated on the 07-01 walkthrough),
(e) live SPA smoke test. Reversing (a)/(c) triggers the silent `USE_MOCK = true`
sample-data bug (RESEARCH Pitfall 1).

Purpose: Eliminate the dead Express attack surface and confirm secrets are correctly
scoped (SEC-03), while guaranteeing the Supabase-native SPA still serves correctly
after removal. The CSP enforce-flip (SEC-02) lands here because it is only safe once
07-01's Report-Only walkthrough confirmed zero live violations.

Output: Express coupling severed from portal-v2/src; `portal/` deleted from git;
`vercel.json` carrying the enforcing CSP with the SPA rewrite intact; a passing
grep-gate, secret gate, and live smoke test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-security-hardening-and-express-removal/07-CONTEXT.md
@.planning/phases/07-security-hardening-and-express-removal/07-RESEARCH.md
@.planning/phases/07-security-hardening-and-express-removal/07-PATTERNS.md
@portal-v2/src/lib/mockData.ts
@portal-v2/src/components/dashboard/CommandPalette.tsx
@portal-v2/vite.config.ts
@portal-v2/.env.example

<interfaces>
<!-- Exact current state verified 2026-06-02. Edit these exactly. -->

mockData.ts lines 24-26 (the bug to gut):
```typescript
const apiBase = (import.meta.env.VITE_API_BASE_URL ?? '').trim();
const forceMock = String(import.meta.env.VITE_USE_MOCK ?? '').toLowerCase() === 'true';
export const USE_MOCK = !apiBase || forceMock;
```
=> Replace with:
```typescript
export const USE_MOCK =
  String(import.meta.env.VITE_USE_MOCK ?? '').toLowerCase() === 'true';
```

CommandPalette.tsx — EXACT current coupling (verified 2026-06-02):
- line 4:  `import { api } from '../../lib/api';`  → DELETE this import line.
- line 5:  `import type { SearchHit } from '../../lib/types';` → KEEP (SearchHit is
           used by the CommandPaletteProps `onSelect` callback + the `hits` state).
- The ONLY `api.*` call site is the effect at lines ~81-93:
  ```typescript
  api
    .search(debounced, scope, 20)
    .then((r) => { if (cancelled) return; setHits(r.hits); setActive(0); })
    .catch((e) => !cancelled && setError(e instanceof Error ? e.message : 'Search failed'))
    .finally(() => !cancelled && setLoading(false));
  ```
  `api.search(...)` returns `Promise<{ hits: SearchHit[]; total: number }>`; the
  component reads `r.hits`. Replace the whole `api.search(...).then(...).catch(...).finally(...)`
  chain with a no-op empty-results path that preserves the same control flow — do NOT
  throw (a throw would surface a spurious error in the dormant palette). Concretely:
  ```typescript
  // Cmd+K search deferred to v2 (CONTEXT.md Deferred); Express /api/search removed in Phase 07.
  void Promise.resolve({ hits: [] as SearchHit[], total: 0 }).then((r) => {
    if (cancelled) return;
    setHits(r.hits);
    setActive(0);
    setLoading(false);
  });
  ```
  Keep the component shell — keyboard shortcut wiring, open/close, the scope tabs,
  the results/empty-state UI, the footer — all intact. The palette renders, opens on
  Cmd+K, and shows the "Type to search…" empty state without crashing.

api.ts importers (all 7 must be severed before deleting api.ts):
- src/hooks/useRuns.ts (DELETE the whole file — dead)
- src/components/dashboard/CommandPalette.tsx (SEVER import + stub api.search — component stays, it IS routed via DashboardLayout line 69)
- src/components/dashboard/ArtifactExplorer.tsx (DELETE — dead, unrouted)
- src/components/dashboard/ArtifactPanel.tsx (DELETE — dead, unrouted)
- src/components/dashboard/FilePreview.tsx (DELETE — dead, unrouted)
- src/components/dashboard/InteractiveExcelView.tsx (DELETE — dead, unrouted)
- src/components/dashboard/StyledExcelView.tsx (DELETE — dead, unrouted)

vite.config.ts: remove the entire `server: { port, proxy: { '/api','/auth','/csrf-token','/health' } }` block (lines ~29-48). Keep plugins/resolve.alias/build.

.env.example: remove VITE_API_BASE_URL, the GITHUB_TOKEN/GITHUB_OWNER/GITHUB_REPO trio, PORT, SESSION_SECRET, NODE_ENV. Keep VITE_SUPABASE_*, VITE_HCAPTCHA_SITEKEY, VITE_DOCS_URL, VITE_SENTRY_*.

vercel.json (after 07-01 Report-Only ships): flip key "Content-Security-Policy-Report-Only" → "Content-Security-Policy"; value identical. Rewrites block unchanged.
ENFORCE-FLIP PRECONDITION: this flip is safe ONLY after 07-01 Task 2's live
zero-violation walkthrough was approved. depends_on: [01] makes Wave 2 wait for plan
07-01 to fully complete (including its blocking human checkpoint); Task 2 Step D
re-confirms that approval before changing the header key.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Sever all portal-v2/src Express coupling (mockData, useRuns, CommandPalette, 5 dead components, vite.config, .env.example)</name>
  <files>portal-v2/src/lib/mockData.ts, portal-v2/src/lib/api.ts, portal-v2/src/hooks/useRuns.ts, portal-v2/src/components/dashboard/CommandPalette.tsx, portal-v2/src/components/dashboard/ArtifactExplorer.tsx, portal-v2/src/components/dashboard/ArtifactPanel.tsx, portal-v2/src/components/dashboard/FilePreview.tsx, portal-v2/src/components/dashboard/InteractiveExcelView.tsx, portal-v2/src/components/dashboard/StyledExcelView.tsx, portal-v2/vite.config.ts, portal-v2/.env.example</files>
  <read_first>
    - portal-v2/src/lib/mockData.ts (lines 1-26 — the USE_MOCK bug)
    - portal-v2/vite.config.ts (the server.proxy block to remove)
    - portal-v2/.env.example (the stale Express vars to delete)
    - portal-v2/src/components/dashboard/CommandPalette.tsx (the one api importer that stays — sever the `import { api } from '../../lib/api'` line + replace the single `api.search(...)` call site at lines ~81-93; KEEP the `SearchHit` type import on line 5)
    - .planning/phases/07-security-hardening-and-express-removal/07-PATTERNS.md §"Express Removal Order" + each per-file Pattern Assignment (mockData, useRuns, CommandPalette, dead components, vite.config, .env.example)
    - .planning/phases/07-security-hardening-and-express-removal/07-RESEARCH.md §"Question 8" (Express coupling table — incl. the CommandPalette stub spec) + Pitfall 1 (the USE_MOCK silent bug)
  </read_first>
  <action>
    Sever EVERY Express coupling from portal-v2/src BEFORE any deletion of portal/.
    Order (per 07-PATTERNS.md §"Express Removal Order"):

    1. Edit `portal-v2/src/lib/mockData.ts`: replace lines 24-26
       (`const apiBase ... export const USE_MOCK = !apiBase || forceMock;`) with:
       ```typescript
       export const USE_MOCK =
         String(import.meta.env.VITE_USE_MOCK ?? '').toLowerCase() === 'true';
       ```
       Update the JSDoc at lines 1-23 to remove the VITE_API_BASE_URL references
       (the mock now activates ONLY when VITE_USE_MOCK === 'true'). Keep the
       MOCK_RUNS/MOCK_ARTIFACTS/MOCK_JOBS sample arrays below line 27 untouched.

    2. `git rm portal-v2/src/hooks/useRuns.ts` — dead (SSE poller to the removed
       Express /api/events; no live importer — only documentation comments in
       DashboardLayout/Navbar reference its prior removal).

    3. Edit `portal-v2/src/components/dashboard/CommandPalette.tsx` (the one api
       importer that STAYS — it is rendered by DashboardLayout):
       - DELETE line 4: `import { api } from '../../lib/api';`.
       - KEEP line 5: `import type { SearchHit } from '../../lib/types';` — SearchHit
         is still referenced by `CommandPaletteProps.onSelect` and the `hits` state.
       - Replace the ONLY `api.*` call site — the `api.search(debounced, scope, 20)
         .then(...).catch(...).finally(...)` chain inside the search `useEffect`
         (lines ~81-93) — with a no-op empty-results path that returns
         `Promise.resolve({ hits: [] as SearchHit[], total: 0 })` (NOT a throw, NOT a
         bare `[]`), preserving the existing `if (cancelled) return; setHits(r.hits);
         setActive(0);` flow and clearing `setLoading(false)`:
         ```typescript
         // Cmd+K search deferred to v2 (CONTEXT.md Deferred); Express /api/search removed in Phase 07.
         void Promise.resolve({ hits: [] as SearchHit[], total: 0 }).then((r) => {
           if (cancelled) return;
           setHits(r.hits);
           setActive(0);
           setLoading(false);
         });
         ```
       Keep the component shell — keyboard shortcut wiring, open/close, scope tabs,
       results/empty-state UI, footer — intact. The palette must open on Cmd+K and
       render the "Type to search…" empty state WITHOUT crashing or throwing.

    4. `git rm` the 5 confirmed-dead, unrouted dashboard components:
       `portal-v2/src/components/dashboard/ArtifactExplorer.tsx`,
       `ArtifactPanel.tsx`, `FilePreview.tsx`, `InteractiveExcelView.tsx`,
       `StyledExcelView.tsx`. (Verified: only cross-reference is a code comment in
       useCommandPalette.ts, not an import.) Before deleting, run the dead-code grep
       in the acceptance criteria to re-confirm zero real importers.

    5. `git rm portal-v2/src/lib/api.ts` — now that all 7 importers are severed.

    6. Edit `portal-v2/vite.config.ts`: remove the entire `server: { port: 5173,
       proxy: { '/api', '/auth', '/csrf-token', '/health' } }` block (~lines 29-48).
       This eliminates the last `VITE_API_BASE_URL` reference in that file. Keep
       imports, plugins, resolve.alias, and build config unchanged.

    7. Edit `portal-v2/.env.example`: delete the stale Express vars —
       `VITE_API_BASE_URL=` and its comment block, the
       `GITHUB_TOKEN`/`GITHUB_OWNER`/`GITHUB_REPO` trio, `PORT=3000`,
       `SESSION_SECRET=...`, `NODE_ENV=development`, and the
       "NOTE: ... stale (Express) — Phase 07 cleanup" line. KEEP VITE_SUPABASE_URL,
       VITE_SUPABASE_ANON_KEY, VITE_HCAPTCHA_SITEKEY (+ its dev/test comment),
       VITE_DOCS_URL, and the VITE_SENTRY_* block.

    Then run the build + tests to confirm nothing live broke.
    DO NOT run `git rm -r portal/` in this task — that is Task 2, gated on the
    grep-gate below.
  </action>
  <verify>
    <automated>cd portal-v2 && npm run build && npm test</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "VITE_USE_MOCK" portal-v2/src/lib/mockData.ts` returns ≥ 1 and `grep -c "VITE_API_BASE_URL" portal-v2/src/lib/mockData.ts` returns 0.
    - `test ! -f portal-v2/src/hooks/useRuns.ts && test ! -f portal-v2/src/lib/api.ts` (both deleted).
    - `test ! -f portal-v2/src/components/dashboard/ArtifactExplorer.tsx` and the other 4 dead components are deleted.
    - `grep -n "from '../../lib/api'" portal-v2/src/components/dashboard/CommandPalette.tsx` returns nothing (the api import is severed) AND `test -f portal-v2/src/components/dashboard/CommandPalette.tsx` (the component file still exists) AND `cd portal-v2 && npm run build` exits 0 (no dangling `api.` reference; the stubbed search compiles).
    - `grep -c "api.search\|api\\.\b" portal-v2/src/components/dashboard/CommandPalette.tsx` returns 0 (no residual `api.*` call site after the stub).
    - `grep -rn "ArtifactExplorer\|ArtifactPanel\|FilePreview\|InteractiveExcelView\|StyledExcelView" portal-v2/src/ --include="*.tsx" --include="*.ts" | grep "import"` returns nothing (no live imports of the deleted components).
    - `grep -c "proxy" portal-v2/vite.config.ts` returns 0 and `grep -c "VITE_API_BASE_URL" portal-v2/vite.config.ts` returns 0.
    - `grep -c "VITE_API_BASE_URL\|SESSION_SECRET\|GITHUB_TOKEN" portal-v2/.env.example` returns 0; `grep -c "VITE_SUPABASE_URL" portal-v2/.env.example` returns ≥ 1.
    - `cd portal-v2 && npm run build` exits 0 and `npm test` passes (no dead-import compile errors; no regression).
  </acceptance_criteria>
  <done>All Express coupling is severed from portal-v2/src; CommandPalette's api.search is stubbed to an empty-results Promise (no crash); mockData USE_MOCK no longer depends on VITE_API_BASE_URL; the build and test suite pass with portal/ still present.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: [BLOCKING] D-02 grep-gate + SEC-03 secret gate, then git rm -r portal/, then CSP enforce-flip (gated on 07-01 walkthrough)</name>
  <files>portal/, portal-v2/vercel.json</files>
  <read_first>
    - .planning/phases/07-security-hardening-and-express-removal/07-RESEARCH.md §"Question 8" (D-02 grep-gate exact commands + git rm command + SPA-rewrite confirmation)
    - .planning/phases/07-security-hardening-and-express-removal/07-PATTERNS.md §"portal-v2/vercel.json" Wave D flip + §"api.ts" grep-gate
    - .planning/phases/07-security-hardening-and-express-removal/07-CONTEXT.md §D-01, §D-02, §D-04
    - .planning/phases/07-security-hardening-and-express-removal/07-01-SUMMARY.md (CONFIRM the Task 2 zero-violation walkthrough was approved BEFORE flipping the CSP — see Step D precondition)
    - portal-v2/vercel.json (the Report-Only header shipped in 07-01 — flip the CSP key)
  </read_first>
  <action>
    This is the BLOCKING removal gate. Execute in strict order; if any grep gate
    returns a match, STOP and fix Task 1 before proceeding.

    PRE-FLIGHT (operator user_setup, RESEARCH Open Questions §2): confirm no live
    Render/Railway (or other) service still roots at portal/ before STEP C — if one
    exists, decommission it first (deletion would 404 it). Note the confirmation in
    the SUMMARY.

    STEP A — D-02 grep-gate (each MUST return EMPTY; non-empty = blocked):
    ```bash
    grep -rn "VITE_API_BASE_URL" portal-v2/src/ --include="*.ts" --include="*.tsx"
    grep -rn "fetch('/api"       portal-v2/src/ --include="*.ts" --include="*.tsx"
    grep -rn 'fetch("/api'       portal-v2/src/ --include="*.ts" --include="*.tsx"
    grep -rn "API_BASE"          portal-v2/src/ --include="*.ts" --include="*.tsx"
    grep -rn "from.*['\"].*lib/api['\"]" portal-v2/src/ --include="*.ts" --include="*.tsx"
    ```

    STEP B — SEC-03 secret gate (MUST return EMPTY when node_modules is excluded;
    the only SERVICE_ROLE hits in the repo are vendored docstrings under
    portal-v2/node_modules, which are not source):
    ```bash
    grep -rn "SERVICE_ROLE" portal-v2/src/ --include="*.ts" --include="*.tsx"
    grep -rn "SERVICE_ROLE" portal-v2/ --include="*.json" | grep -v node_modules
    ```
    Both must be empty. (SEC-03 also requires VITE_API_BASE_URL removed from Vercel
    env vars — that is the operator user_setup task; note it in the SUMMARY.)

    STEP C — only after the pre-flight + A + B are clean, delete the Express backend:
    ```bash
    git rm -r portal/
    ```

    STEP D — confirm the 07-01 walkthrough passed, confirm the SPA rewrite survived,
    THEN flip CSP to enforcing.

    ENFORCE-FLIP PRECONDITION (BLOCKER — do NOT flip without this): before changing
    the CSP header key, CONFIRM that 07-01 Task 2's live zero-violation walkthrough
    was approved — verify the 07-01-SUMMARY.md records the Report-Only walkthrough as
    PASS (zero CSP console violations across Realtime ws, hCaptcha, Sentry, signed-URL
    download, and Vite assets). The enforcing CSP actively BLOCKS non-allowlisted
    origins, so flipping before that walkthrough passed would break live functionality.
    `depends_on: [01]` already makes Wave 2 wait for plan 07-01 to fully complete
    (including its blocking human checkpoint); this step is the in-plan fail-safe
    re-confirmation. If the 07-01-SUMMARY does NOT record an approved zero-violation
    walkthrough, STOP — do not flip the header.

    Then, in `portal-v2/vercel.json`, confirm the `rewrites` block is still exactly
    `{ "source": "/(.*)", "destination": "/index.html" }`, and flip the CSP header:
    change the key `"Content-Security-Policy-Report-Only"` to
    `"Content-Security-Policy"`. The value string is IDENTICAL — only the key name
    changes. All 4 named headers stay unchanged.

    STEP E — rebuild to confirm the SPA still compiles with portal/ gone:
    ```bash
    cd portal-v2 && npm run build
    ```
  </action>
  <verify>
    <automated>cd "C:\Users\juflores\dev\Generate-Weekly-PDFs-DSR-Resiliency" && bash -c 'set -e; m=$(grep -rn "VITE_API_BASE_URL\|API_BASE\|fetch(.\/api" portal-v2/src/ --include="*.ts" --include="*.tsx" || true); if [ -n "$m" ]; then echo "FAIL grep-gate: $m"; exit 1; fi; s=$(grep -rn "SERVICE_ROLE" portal-v2/src/ --include="*.ts" --include="*.tsx" || true); if [ -n "$s" ]; then echo "FAIL secret-gate: $s"; exit 1; fi; if [ -d portal ]; then echo "FAIL: portal/ still exists"; exit 1; fi; sf=.planning/phases/07-security-hardening-and-express-removal/07-01-SUMMARY.md; if [ ! -f "$sf" ]; then echo "FAIL: 07-01-SUMMARY.md missing — cannot confirm the zero-violation walkthrough before enforce-flip"; exit 1; fi; if ! grep -qiE "zero[- ]violation|zero CSP|PASS.*walkthrough|walkthrough.*(pass|approved)" "$sf"; then echo "FAIL: 07-01-SUMMARY does not record an approved zero-violation walkthrough — enforce-flip precondition unmet"; exit 1; fi; if ! grep -q "\"Content-Security-Policy\"" portal-v2/vercel.json; then echo "FAIL: CSP not flipped to enforcing"; exit 1; fi; if grep -q "Content-Security-Policy-Report-Only" portal-v2/vercel.json; then echo "FAIL: Report-Only still present"; exit 1; fi; if ! grep -q "/index.html" portal-v2/vercel.json; then echo "FAIL: SPA rewrite missing"; exit 1; fi; echo "PASS: grep-gate empty, secret-gate empty, portal/ deleted, 07-01 walkthrough confirmed, CSP enforcing, rewrite intact"'</automated>
  </verify>
  <acceptance_criteria>
    - All 5 D-02 grep-gate commands return nothing (exit 1 from grep = no match).
    - `grep -rn "SERVICE_ROLE" portal-v2/src/` returns nothing AND `grep -rn "SERVICE_ROLE" portal-v2/ --include="*.json" | grep -v node_modules` returns nothing (SEC-03).
    - The operator pre-flight confirmed no live Render/Railway (or other) service roots at portal/ before the `git rm` (recorded in the SUMMARY).
    - `test ! -d portal/` — the Express backend directory is deleted from the working tree and git index.
    - ENFORCE-FLIP PRECONDITION: the 07-01-SUMMARY.md records 07-01 Task 2's live zero-violation walkthrough as PASS/approved BEFORE the CSP key is flipped; the verify gate fails if that evidence is absent. The header is NOT flipped to enforcing without it.
    - `grep -c '"Content-Security-Policy"' portal-v2/vercel.json` returns 1 and `grep -c "Content-Security-Policy-Report-Only" portal-v2/vercel.json` returns 0 (enforce-flip done).
    - The `rewrites` block in vercel.json is unchanged: `{ "source": "/(.*)", "destination": "/index.html" }`.
    - `cd portal-v2 && npm run build` exits 0 with portal/ removed.
  </acceptance_criteria>
  <done>The operator confirmed no orphaned Render/Railway service, the D-02 grep-gate and SEC-03 secret gate are both empty, portal/ is git-removed, the 07-01 zero-violation walkthrough is confirmed approved, the CSP is flipped to enforcing, the SPA rewrite is intact, and the build passes.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: [BLOCKING] Live SPA smoke test after Express removal + enforcing CSP (D-02)</name>
  <files>portal-v2/vercel.json (live deploy observed; no file change in this task)</files>
  <read_first>
    - .planning/phases/07-security-hardening-and-express-removal/07-RESEARCH.md §"Question 8" (Live smoke-test sequence) + Pitfall 1 (USE_MOCK silent sample-data bug)
    - .planning/phases/07-security-hardening-and-express-removal/07-CONTEXT.md §D-02
  </read_first>
  <action>
    After the post-removal + enforcing-CSP deploy goes live on Vercel, run the live
    SPA smoke test described in how-to-verify. This task changes no files — it
    confirms the Supabase-native SPA still serves correctly (real data, working
    download, deep-link survives) and that the enforcing CSP blocked nothing. Record
    the outcome for 07-04's 07-SECURITY.md smoke-test evidence.
  </action>
  <what-built>
    portal/ is deleted, all Express coupling is severed from portal-v2/src, and the
    CSP is now ENFORCING (not Report-Only) on the live deploy. This BLOCKING
    checkpoint confirms the Supabase-native SPA still serves correctly and that the
    enforcing CSP did not break any live origin.
  </what-built>
  <how-to-verify>
    Wait for the new deploy (post git rm + enforcing CSP) to go live on Vercel, then:

    1. Navigate to the Vercel URL while logged out → redirects to /login (NOT a 404).
    2. Log in with billing/admin credentials → /dashboard loads.
    3. The artifact table shows REAL Supabase rows — NOT sample/mock data. (If you
       see mock rows, the USE_MOCK bug from Pitfall 1 regressed — STOP and re-check
       Task 1's mockData edit + that VITE_API_BASE_URL was removed from Vercel env.)
    4. Click download on an artifact → the signed URL resolves and the file downloads.
    5. Hard-refresh the page AT /dashboard (deep link) → the SPA rewrite catches it →
       no 404, the table reloads.
    6. With the enforcing CSP now live, open DevTools Console and repeat the 07-01
       walkthrough origins (Realtime ws, hCaptcha, Sentry, download) → confirm NO
       blocked-resource CSP errors (now enforcing, a violation would actually break
       functionality). Everything must still work. Open the Cmd+K palette → it shows
       the "Type to search…" empty state and does NOT crash (search is stubbed/dormant).

    Record PASS or list each failure. Capture for 07-04's 07-SECURITY.md.
  </how-to-verify>
  <verify>
    <automated>echo "Manual BLOCKING checkpoint: live SPA smoke test. Automated phase-gate re-confirmation in 07-04: curl -sI <VERCEL_URL>/dashboard returns 200/3xx (not 404); curl -sI <VERCEL_URL> | grep -i 'content-security-policy:' shows the enforcing header."</automated>
  </verify>
  <acceptance_criteria>
    - Logged-out visit redirects to /login (not 404); login reaches /dashboard.
    - The artifact table renders REAL Supabase rows (not mock data) — confirms the USE_MOCK fix + Vercel env cleanup held.
    - An artifact download via signed URL succeeds.
    - A hard refresh at the /dashboard deep link does not 404 (SPA rewrite intact).
    - With the enforcing CSP live, no resource (Realtime ws, hCaptcha, Sentry, signed-URL download, Vite assets) is blocked in the DevTools Console; the Cmd+K palette opens to its empty state without crashing.
    - The outcome is recorded for 07-04's 07-SECURITY.md smoke-test evidence.
  </acceptance_criteria>
  <done>The live deploy passes all 6 smoke-test steps with the enforcing CSP and real (non-mock) data; the outcome is recorded for 07-04's 07-SECURITY.md.</done>
  <resume-signal>Type "approved" once the live deploy passes all 6 smoke-test steps with the enforcing CSP and real (non-mock) data, or describe each failure.</resume-signal>
</task>

</tasks>

<threat_model>
ASVS Level: L1. Block on: HIGH severity. Primary ASVS focus: V14 Configuration, V4 Access Control.

## Trust Boundaries

| Boundary | Description | Data Crossing |
|----------|-------------|---------------|
| repo / build → deployed bundle | Dead Express code + stale secrets must not ship; the bundle must read Supabase directly, never a removed /api. | Frontend source + env config |
| Vercel env → frontend bundle | service_role must never be present; VITE_API_BASE_URL must be absent so USE_MOCK cannot silently flip true. | Public VITE_* env vars only |
| browser → Vercel Edge | Enforcing CSP now actively blocks any non-allowlisted origin. | All portal responses |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-04-dead-surface | Elevation of Privilege | Legacy Express backend portal/ | mitigate | `git rm -r portal/` removes the entire dead attack surface (server.js, session/CSRF auth, SSE, GitHub-token API). Gated behind the D-02 grep-gate (Task 2 Step A). |
| T-07-04-secret-leak | Information Disclosure | service_role / secrets in frontend | mitigate | SEC-03 secret gate (Task 2 Step B): `grep SERVICE_ROLE portal-v2/src` + non-node_modules JSON → empty; operator removes VITE_API_BASE_URL from Vercel env. |
| T-07-04-silent-mock | Tampering (integrity of displayed data) | mockData USE_MOCK | mitigate | USE_MOCK regated on explicit VITE_USE_MOCK only (Task 1); Task 3 smoke test confirms real (non-mock) rows render — closes RESEARCH Pitfall 1. |
| T-07-04-spa-404 | DoS (availability) | vercel.json SPA rewrite | mitigate | Rewrite `{ "source": "/(.*)", "destination": "/index.html" }` preserved through the edit (Task 2 Step D); Task 3 confirms /dashboard deep-link does not 404. |
| T-07-04-csp-enforce-breakage | DoS (self-inflicted) | enforcing CSP | mitigate | Enforce-flip is gated on 07-01's zero-violation Report-Only walkthrough (depends_on: 01 + Task 2 Step D in-plan precondition that re-confirms the 07-01-SUMMARY records an approved walkthrough); Task 3 re-confirms no blocked resources under enforcement. |
| T-07-04-orphan-service | DoS (external) | Render/Railway service on portal/ | accept→verify | If an external service still roots at portal/ it 404s after deletion (RESEARCH A4 / Open Questions §2). Operator user_setup + Task 2 pre-flight confirms/decommissions before the git rm. |
| T-07-04-residual-coupling | Tampering | portal-v2/src dead imports | mitigate | D-02 grep-gate (Task 2 Step A) blocks deletion until VITE_API_BASE_URL / fetch('/api' / lib/api imports are all gone. |
</threat_model>

<verification>
- Task 1: `npm run build` + `npm test` pass with all coupling severed (portal/ still present); CommandPalette's api.search stubbed (no residual `api.*`).
- Task 2 (BLOCKING): grep-gate + secret-gate both empty; portal/ deleted; 07-01 zero-violation walkthrough confirmed in the 07-01-SUMMARY before the flip; CSP enforcing; rewrite intact; build passes — all asserted by the bash gate.
- Task 3 (BLOCKING): live smoke test — login → real rows → download → deep-link refresh → no CSP-blocked resources under enforcement; Cmd+K palette opens to empty state without crashing.
- Phase gate (07-04): `curl -sI <VERCEL_URL>/dashboard` returns 200/3xx (not 404); `curl -sI <VERCEL_URL> | grep -i "content-security-policy:"` shows the enforcing header.
</verification>

<success_criteria>
- All Express coupling severed from portal-v2/src; mockData USE_MOCK gated only on VITE_USE_MOCK; CommandPalette's api.search stubbed to an empty-results Promise.
- D-02 grep-gate + SEC-03 secret gate both empty; portal/ deleted from git.
- The CSP enforce-flip happens ONLY after the 07-01 zero-violation walkthrough is confirmed approved (Task 2 Step D precondition).
- vercel.json SPA rewrite intact; CSP flipped to enforcing Content-Security-Policy.
- Live smoke test passes (real data, working download, deep-link survives, no CSP breakage).
- Operator has removed VITE_API_BASE_URL from Vercel env (user_setup) and confirmed no orphaned Render/Railway service before the git rm.
</success_criteria>

<output>
After completion, create `.planning/phases/07-security-hardening-and-express-removal/07-03-SUMMARY.md` recording: the grep-gate + secret-gate outputs (empty), the git rm commit, the confirmation that the 07-01 zero-violation walkthrough was approved before the CSP flip, the CSP enforce-flip, the live smoke-test outcome, and the operator's Vercel-env + Render/Railway confirmations — for 07-04's 07-SECURITY.md SEC-03 Verification + smoke-test sections.
</output>
