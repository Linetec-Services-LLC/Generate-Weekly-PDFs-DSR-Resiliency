---
phase: 1.1
slug: subcontractor-helper-shadow-rescue
status: validated
nyquist_compliant: true
wave_0_complete: true
created: 2026-05-19
validated: 2026-05-26
---

# Phase 1.1 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | pytest 7.x (existing) |
| **Config file** | none — Wave 0 not required (existing infrastructure covers all phase requirements) |
| **Quick run command** | `pytest tests/test_subcontractor_pricing.py -v` |
| **Full suite command** | `pytest tests/ -v` |
| **Estimated runtime** | ~25-40 seconds (quick) / ~90 seconds (full) |

---

## Sampling Rate

- **After every task commit:** Run `pytest tests/test_subcontractor_pricing.py -v` (or the focused file modified by the task)
- **After every plan wave:** Run `pytest tests/ -v`
- **Before `/gsd-verify-work`:** Full suite must be green
- **Max feedback latency:** 40 seconds (quick) / 90 seconds (full)

---

## Per-Task Verification Map

Populated retroactively (State A audit, 2026-05-26) from the 6 PLAN/SUMMARY
artifacts + 01.1-VERIFICATION.md against the shipped test suite. One row per
requirement (SUB-08..12); each row pins the covering test class to the delivering
plan. No SECURITY.md for this phase → Threat Ref `—`.

| Task ID | Plan | Wave | Requirement | Threat Ref | Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|------------|----------|-----------|-------------------|-------------|--------|
| SUB-08 | 01.1-01 | 1 | SUB-08 | — | Pre-acceptance rescue (`_subcontractor_rescue_price`) so blank-price helper rows survive the acceptance gate; kill switch `SUBCONTRACTOR_RATE_RECALC_PREACCEPTANCE_ENABLED` | unit + e2e | `pytest tests/test_subcontractor_helper_shadow_rescue.py::TestEndToEndPipeline -q` | ✅ | ✅ green |
| SUB-09 | 01.1-02, 01.1-06 | 2 | SUB-09 | — | Subcontractor non-helper rows emit ONLY variant keys; helper rows emit ONLY shadow keys (`if not is_subcontractor_row:` partition guards) — no legacy primary/`_Helper_` file | e2e | `pytest tests/test_subcontractor_helper_shadow_rescue.py::TestEndToEndPipeline tests/test_subcontractor_helper_shadow_rescue.py::TestLegacyHelperTargetCleanupE2E -q` | ✅ | ✅ green |
| SUB-10 | 01.1-03 | 2 | SUB-10 | — | `cleanup_untracked_sheet_attachments` `variant_whitelist`; PPP pass prunes off-contract variants unconditionally | e2e | `pytest tests/test_subcontractor_helper_shadow_rescue.py::TestBugB2WhitelistE2E -q` ; `pytest tests/test_security_audit_followup.py::TestPppCleanupUntrackedAttachments -q` | ✅ | ✅ green |
| SUB-11 | 01.1-04 | 2 | SUB-11 | — | Per-row claim-history attribution via `lookup_attribution` reader; D-12 fall-back-to-current-helper when RPC absent | unit | `pytest tests/test_billing_audit_shadow.py::TestLookupAttribution -q` (4 postgrest-gated cases skip on dev) | ✅ | ✅ green |
| SUB-12 | 01.1-05, 01.1-06 | 2 | SUB-12 | — | One-time idempotent hash-prune (`PHASE_1_1_HASH_PRUNE_VERSION = 2`); drops orphan primary + 6-part helper keys for sub WRs | unit | `pytest tests/test_subcontractor_helper_shadow_rescue.py::TestHashPruneIdempotency tests/test_subcontractor_helper_shadow_rescue.py::TestProductionCodeSiteInvariants -q` | ✅ | ✅ green |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

Sampling continuity: every requirement (SUB-08..12) has a passing automated
command; no run of 3 consecutive items lacks an automated verify. The 4
`TestLookupAttribution` postgrest-gated cases skip on dev environments without
`postgrest` installed (documented in CLAUDE.md) — the non-gated SUB-11 assertions
still run green.

---

## Wave 0 Requirements

*Existing infrastructure covers all phase requirements. No Wave 0 stubs needed — the pytest harness, `tests/test_subcontractor_pricing.py`, `tests/test_billing_audit_shadow.py`, `tests/test_security_audit_followup.py`, `tests/test_performance_optimizations.py`, and `tests/validate_production_safety.py` are all already in place from Phase 1.*

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| Data-team coordination for `billing_audit.attribution_snapshot` access | SUB-11 (Bug C) | RPC creation OR PostgREST exposed-schemas change is owned by the data team, not this codebase. Must be confirmed at merge time. | Confirm `lookup_attribution(p_wr, p_week_ending, p_smartsheet_row_id)` RPC exists in production Supabase (or that direct `SELECT` on `billing_audit.attribution_snapshot` returns) before flipping `SUBCONTRACTOR_HELPER_CLAIM_ATTRIBUTION_ENABLED=1`. The code's fall-back-to-current-helper path (D-12) makes Bug C safe-to-merge before this confirmation, but the feature only activates after the data team's side ships. |
| Cron-run byte-identical guarantee (ROADMAP success criterion #5) | SUB-08..12 | Requires a real scheduled GHA run against production Smartsheet (the synthetic-mock unit tests cannot exercise the real source-sheet diversity). | After merge, watch the first 2 cron runs and verify that primary / helper / vac_crew / ORIG-folder Excel attachments produce the same hashes as the pre-merge baseline (hash-history diff in `generated_docs/hash_history.json`). Only subcontractor variant outputs should change. |
| Hash-history one-time prune fires once and then no-ops | SUB-12 | The version-advance trigger is observable across a session boundary, not within a single test run. | Verify the first post-merge cron run logs ONE INFO line naming the orphan count + WR list; subsequent runs in the same `PHASE_1_1_HASH_PRUNE_VERSION` band emit zero such lines. |

---

## Validation Sign-Off

- [x] All tasks have `<automated>` verify or Wave 0 dependencies
- [x] Sampling continuity: no 3 consecutive tasks without automated verify
- [x] Wave 0 covers all MISSING references *(N/A — no Wave 0 needed; existing infra covers all)*
- [x] No watch-mode flags *(`pytest -q` is one-shot, not watch)*
- [x] Feedback latency < 90s *(SUB-08..12 files run in <1s)*
- [x] `nyquist_compliant: true` set in frontmatter

**Approval:** validated (all tasks green; retroactive State-A audit 2026-05-26)

---

## Validation Audit 2026-05-26

Retroactive State-A audit (VALIDATION.md existed as an unpopulated draft —
per-task map was `TBD`). Built the SUB-08..12 requirement-to-test map from the 6
PLAN/SUMMARY artifacts + 01.1-VERIFICATION.md, confirmed every covering test
class is present (`grep ^class`), and ran every automated command. No gaps —
every requirement has passing automated coverage, so no `gsd-nyquist-auditor`
spawn was required.

| Metric | Count |
|--------|-------|
| Gaps found | 0 |
| Resolved | 0 |
| Escalated (manual-only) | 3 (pre-declared: data-team `lookup_attribution` RPC deploy, cron byte-identical guarantee, one-time hash-prune observation) |

**Evidence (commands run 2026-05-26):**

| Check | Result |
|-------|--------|
| `pytest tests/test_subcontractor_helper_shadow_rescue.py tests/test_billing_audit_shadow.py::TestLookupAttribution -q` | 56 passed, 4 skipped (postgrest-gated) |
| 6 Phase-01.1 test classes present (`grep ^class`) | all found (TestEndToEndPipeline, TestBugB2WhitelistE2E, TestLegacyHelperTargetCleanupE2E, TestHashPruneIdempotency, TestProductionCodeSiteInvariants, TestLookupAttribution) |
| SUB-08 rescue unit tests (`test_bug_a_rescue_*`) + production-site guard | PASS |

**Conclusion:** Phase 01.1 is Nyquist-compliant. The 3 manual-only verifications
are correctly deferred (data-team Supabase RPC deploy, live cron byte-identical
diff, one-time hash-prune session-boundary observation), not validation gaps. The
4 skipped `TestLookupAttribution` cases are postgrest-gated on dev environments —
expected, documented in CLAUDE.md.
