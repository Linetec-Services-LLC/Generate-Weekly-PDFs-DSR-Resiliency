---
phase: 06-realtime-and-ui-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - portal-v2/package.json
  - portal-v2/package-lock.json
  - portal-v2/src/test/setup.ts
autonomous: false
requirements: [DATA-06, UI-03]
user_setup:
  - service: supabase
    why: "DATA-06 requires the artifacts table in the supabase_realtime publication; without it INSERT events never arrive (silent false-positive)."
    dashboard_config:
      - task: "Run the publication-verify SQL, then (if absent) the ALTER PUBLICATION SQL, in the SQL Editor for project poeyztlmsawfoqlanucc"
        location: "Supabase Dashboard → project poeyztlmsawfoqlanucc → SQL Editor"

must_haves:
  truths:
    - "public.artifacts is a member of the supabase_realtime publication on project poeyztlmsawfoqlanucc (DATA-06 — gates all Realtime work; without it INSERT events never arrive and DATA-06 silently fails)."
    - "jest-axe is installed as a dev dependency and wired into the vitest setup so component-level a11y assertions run on every push (D-07 automated pass / UI-03)."
    - "The publication-verify SQL result is recorded so the gate is auditable for Phase 07."
  artifacts:
    - path: "portal-v2/src/test/setup.ts"
      provides: "jest-axe toHaveNoViolations matcher registered globally for vitest"
      contains: "expect.extend(toHaveNoViolations)"
    - path: "portal-v2/package.json"
      provides: "jest-axe + @types/jest-axe dev dependencies"
      contains: "jest-axe"
  key_links:
    - from: "supabase_realtime publication"
      to: "public.artifacts"
      via: "ALTER PUBLICATION supabase_realtime ADD TABLE public.artifacts (idempotent verify-then-add)"
      pattern: "supabase_realtime.*artifacts"
    - from: "portal-v2/src/test/setup.ts"
      to: "jest-axe"
      via: "import { toHaveNoViolations } + expect.extend"
      pattern: "toHaveNoViolations"
---

<objective>
Foundation wave for Phase 06: enable Supabase Realtime delivery for `public.artifacts`
and install the jest-axe a11y regression net. Both are prerequisites the rest of the
phase silently depends on.

Purpose: DATA-06's entire Realtime surface is dead until `public.artifacts` is a
member of the `supabase_realtime` publication — and it can pass build/types/unit-tests
green while never delivering a single INSERT event (false-positive verification, per
RESEARCH.md finding #2). UI-03's automated WCAG pass (D-07) needs `jest-axe` registered
in the vitest setup before any `*.a11y.test.tsx` can run.

Output: `artifacts` confirmed in the `supabase_realtime` publication (verification
query result recorded); `jest-axe` + `@types/jest-axe` dev deps installed; two lines
added to `src/test/setup.ts` registering `toHaveNoViolations`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-realtime-and-ui-polish/06-RESEARCH.md
@.planning/phases/06-realtime-and-ui-polish/06-VALIDATION.md

<interfaces>
<!-- The live project, confirmed in STATE.md §Infrastructure Topology -->
LIVE Supabase project = `poeyztlmsawfoqlanucc` ("Smarthsheet-Resiliency-Offloaded-Data").
This is the ONLY project with BOTH public.profiles AND public.artifacts (~2,383 rows,
CI publish live). Ignore the red-herring project `iixetbhhntwjinnwoegi`.

Current src/test/setup.ts is exactly one line:
```typescript
import '@testing-library/jest-dom';
```

Deployed RLS policy on artifacts (from portal_schema.sql — already correct, do NOT change):
```sql
CREATE POLICY artifacts_select_billing_or_admin ON public.artifacts
    FOR SELECT TO authenticated
    USING (public.current_user_role() IN ('admin','billing'));
```
RESEARCH.md confirmed Supabase enforces this RLS per-subscriber on postgres_changes —
the publication add does NOT expose rows to pending/anon (D-04 stays clean for Phase 07).
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: [BLOCKING] Verify + enable artifacts in the supabase_realtime publication</name>
  <read_first>
    - .planning/phases/06-realtime-and-ui-polish/06-RESEARCH.md (§"D-04: Supabase Realtime RLS Authorization Model" → "Exact Enablement SQL"; §"Common Pitfalls" → Pitfall 1; §"Open Questions" #1)
    - .planning/STATE.md (§"Infrastructure Topology … READ BEFORE PHASE 05" — confirms live project poeyztlmsawfoqlanucc)
    - .planning/phases/06-realtime-and-ui-polish/06-VALIDATION.md (§"Wave 0 Requirements" — the live SQL verify gating DATA-06)
  </read_first>
  <what-built>
    This is the [BLOCKING] DATA-06 enablement gate. The agent first attempts the
    verification + ALTER via the Supabase MCP (`apply_migration` / SQL execution) against
    project `poeyztlmsawfoqlanucc`. If MCP write access is unavailable, it falls back to
    presenting the exact SQL for the operator to run in the SQL Editor. The publication
    membership is what makes Realtime INSERT events actually arrive — without it the whole
    phase's Realtime code passes tests green but delivers nothing in production.
  </what-built>
  <action>
    1. FIRST attempt automation via Supabase MCP against project `poeyztlmsawfoqlanucc`.
       Run the VERIFY query first:
       ```sql
       SELECT tablename FROM pg_publication_tables
       WHERE pubname = 'supabase_realtime';
       ```
       - If `artifacts` already appears in the result: record "already enabled" and the
         full row list. No ALTER needed (it would be a harmless no-op).
       - If `artifacts` is ABSENT: apply
         ```sql
         ALTER PUBLICATION supabase_realtime ADD TABLE public.artifacts;
         ```
         then re-run the VERIFY query and confirm `artifacts` now appears.
    2. If MCP write access is NOT available (read-only token or apply_migration blocked),
       present the two SQL statements above to the operator and pause for them to run in
       the SQL Editor (Supabase Dashboard → project `poeyztlmsawfoqlanucc` → SQL Editor),
       then paste back the VERIFY query result.
    3. Record the final VERIFY query output (the table list) verbatim in the plan SUMMARY
       under a "Realtime publication verification" heading so the gate is auditable for the
       Phase 07 security review.
    NOTE (D-04, security-clean by construction): adding `artifacts` to the publication does
    NOT bypass RLS — RESEARCH.md confirmed Supabase runs `has_column_privilege` per-subscriber
    on postgres_changes, so pending/anon sockets still receive zero payload. Do NOT add any
    other table (no wildcard); add ONLY `public.artifacts`. Do NOT touch profiles.
  </action>
  <how-to-verify>
    1. The VERIFY query `SELECT tablename FROM pg_publication_tables WHERE pubname = 'supabase_realtime'`
       returns a result set that includes `artifacts`.
    2. The result is pasted into the SUMMARY under "Realtime publication verification".
  </how-to-verify>
  <acceptance_criteria>
    - The VERIFY query output includes the row `artifacts` (confirmed via MCP result or operator paste-back).
    - Only `public.artifacts` was added to the publication — no wildcard, no `profiles`.
    - The SUMMARY contains the verbatim VERIFY query result under a "Realtime publication verification" heading.
  </acceptance_criteria>
  <resume-signal>Type "verified" with the VERIFY query result pasted, or "enabled" if you ran the ALTER and re-verified.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Install jest-axe and wire it into the vitest setup</name>
  <read_first>
    - portal-v2/src/test/setup.ts (the file being modified — currently a single line `import '@testing-library/jest-dom';`)
    - .planning/phases/06-realtime-and-ui-polish/06-PATTERNS.md (§"src/test/setup.ts (MODIFY)" — the exact 2-line add)
    - .planning/phases/06-realtime-and-ui-polish/06-RESEARCH.md (§"Code Examples" → "jest-axe Setup in Existing Vitest Infrastructure"; §"Common Pitfalls" → Pitfall 5 jsdom color-contrast)
    - portal-v2/vitest.config.ts (confirm setupFiles already references src/test/setup.ts — no config change needed)
  </read_first>
  <action>
    1. From `portal-v2/`, install the test-only dev dependencies (D-07 / RESEARCH.md
       §Standard Stack pins jest-axe@10.0.0):
       ```bash
       cd portal-v2 && npm install -D jest-axe@10.0.0 @types/jest-axe
       ```
       Do NOT add any runtime dependency. Do NOT upgrade framer-motion, supabase-js, or
       any TanStack package (RESEARCH.md: stay on framer-motion 11.x).
    2. Edit `portal-v2/src/test/setup.ts` to append exactly two lines after the existing
       jest-dom import, so the full file reads:
       ```typescript
       import '@testing-library/jest-dom';
       import { toHaveNoViolations } from 'jest-axe';   // D-07: WCAG a11y matcher
       expect.extend(toHaveNoViolations);               // D-07
       ```
       `expect` is the global vitest expect (vitest.config has `globals: true` — verify in
       vitest.config.ts; if globals are NOT enabled, import `{ expect }` from 'vitest' at the
       top of setup.ts instead).
    3. Add a code comment noting that jsdom silently disables the axe `color-contrast` rule,
       so contrast validation is the MANUAL pass (D-07 second pass / RESEARCH.md Pitfall 5),
       not jest-axe.
  </action>
  <verify>
    <automated>cd portal-v2 && npm test</automated>
  </verify>
  <acceptance_criteria>
    - `portal-v2/package.json` `devDependencies` contains `jest-axe` and `@types/jest-axe`.
    - `portal-v2/src/test/setup.ts` contains the string `import { toHaveNoViolations } from 'jest-axe'`.
    - `portal-v2/src/test/setup.ts` contains the string `expect.extend(toHaveNoViolations)`.
    - `cd portal-v2 && npm test` exits 0 (the existing suite still passes; the matcher is registered without breaking any test).
  </acceptance_criteria>
  <done>jest-axe installed, setup.ts registers toHaveNoViolations, full vitest suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Supabase Realtime WebSocket → browser | INSERT change events cross from the DB to subscribed clients; RLS must gate per-subscriber |
| npm registry → build | jest-axe + @types/jest-axe pulled into the dev dependency tree |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Information Disclosure | `supabase_realtime` publication membership | mitigate | Add ONLY `public.artifacts` (no wildcard, not `profiles`); RESEARCH.md confirmed Supabase runs `has_column_privilege` per-subscriber on postgres_changes so RLS (`artifacts_select_billing_or_admin`) still withholds payloads from pending/anon. The add does not weaken access control. |
| T-06-02 | Tampering | jest-axe dev dependency supply chain | accept | Test-only dev dep, never shipped to the browser bundle; version pinned to 10.0.0 (RESEARCH.md verified npm 2026-06-02). Low-value target, no production code path. |
| T-06-03 | Elevation of Privilege | leftover `service_role` reaching frontend via this enablement | accept | No `service_role` is introduced here; the publication change is applied via dashboard/MCP against the project, never in portal-v2. Project decision: `service_role` only in CI/Supabase. |
</threat_model>

<verification>
- VERIFY SQL shows `artifacts` in the `supabase_realtime` publication (recorded in SUMMARY).
- `cd portal-v2 && npm test` exits 0.
- `portal-v2/package.json` devDependencies include `jest-axe`.
</verification>

<success_criteria>
- DATA-06 Realtime delivery is unblocked: `public.artifacts` is in the `supabase_realtime` publication and the result is recorded.
- UI-03 automated a11y pass is unblocked: `jest-axe` is installed and `toHaveNoViolations` is registered globally in vitest.
- No runtime dependency added; framer-motion / supabase-js / TanStack versions unchanged.
</success_criteria>

<output>
After completion, create `.planning/phases/06-realtime-and-ui-polish/06-01-SUMMARY.md`
(include the verbatim publication VERIFY query result).
</output>
