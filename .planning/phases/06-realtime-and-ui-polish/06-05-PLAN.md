---
phase: 06-realtime-and-ui-polish
plan: 05
type: execute
wave: 4
depends_on: [06-02, 06-03, 06-04]
files_modified:
  - portal-v2/src/components/artifacts/ArtifactTable.tsx
  - portal-v2/src/components/artifacts/ArtifactTableRow.tsx
  - portal-v2/src/components/artifacts/ArtifactCard.tsx
  - portal-v2/src/components/artifacts/NewArtifactPill.tsx
  - portal-v2/src/components/artifacts/ArtifactSearchBar.tsx
  - portal-v2/src/components/artifacts/VariantFilterBar.tsx
  - portal-v2/src/components/artifacts/ArtifactEmptyState.tsx
  - .planning/phases/06-realtime-and-ui-polish/06-DESIGN-POLISH.md
  - .planning/phases/06-realtime-and-ui-polish/06-HUMAN-UAT.md
autonomous: false
requirements: [UI-01, UI-02, UI-03]

must_haves:
  truths:
    - "The /frontend-design pass runs as an execution-polish layer ONLY — microinteractions, hover/focus states, density rhythm, empty/loading/error refinement — strictly WITHIN the locked UI-SPEC tokens (brand-red, glass depth, 2-weight/3-size typography, 4px spacing, layout). It does NOT change the UI-SPEC's visual identity or layout (D-01)."
    - "The polish proposal is presented for operator approval BEFORE any change lands; nothing is silently applied (D-02 / propose-then-approve)."
    - "Any /frontend-design idea that conflicts with a locked UI-SPEC token is captured as a deferred v2 visual-refresh idea, NOT applied (D-01)."
    - "WCAG AA is verified by BOTH passes: jest-axe automated assertions exist on the new components (regression net), AND a manual keyboard/focus-ring/screen-reader + color-contrast walkthrough is performed at phase close against the UI-SPEC §Accessibility Contract (D-07). jsdom silently disables axe color-contrast, so contrast is the manual pass."
  artifacts:
    - path: ".planning/phases/06-realtime-and-ui-polish/06-DESIGN-POLISH.md"
      provides: "The propose-then-approve polish proposal + the deferred-v2 capture list + operator approval record"
      contains: "Approval"
    - path: ".planning/phases/06-realtime-and-ui-polish/06-HUMAN-UAT.md"
      provides: "Manual UAT checklist: live Realtime, keyboard nav, screen-reader, color-contrast, responsive, reduced-motion"
      contains: "Color-contrast"
  key_links:
    - from: ".planning/phases/06-realtime-and-ui-polish/06-DESIGN-POLISH.md"
      to: "approved polish edits within UI-SPEC tokens"
      via: "operator approval gate before landing"
      pattern: "Approval"
    - from: "jest-axe + manual walkthrough"
      to: "WCAG AA verification"
      via: "D-07 both-passes"
      pattern: "WCAG"
---

<objective>
Run the mandated `/frontend-design` execution-polish pass (propose-then-approve, D-01/D-02)
over the assembled artifact-table + search/filter surface, and perform the D-07 manual
WCAG-AA walkthrough at phase close. This is the FINAL wave — the structure is fully in
place (Realtime, responsive, animations) from Plans 01-04.

Purpose: The ROADMAP build note mandates invoking `/frontend-design` for the artifact
table + search/filter surface (Phase 05 was deliberately functional-only; the distinctive
pass was deferred here). D-01/D-02 constrain it to a polish layer WITHIN the approved
UI-SPEC tokens, presented for approval before landing. D-07 requires a manual pass to cover
what jest-axe cannot (color-contrast, real keyboard/focus, screen-reader announcement,
responsive viewport, reduced-motion).

Output: `06-DESIGN-POLISH.md` (proposal + deferred-v2 list + approval record), approved
polish edits landed within the locked tokens, and `06-HUMAN-UAT.md` (the manual checklist).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-realtime-and-ui-polish/06-UI-SPEC.md
@.planning/phases/06-realtime-and-ui-polish/06-RESEARCH.md
@.planning/phases/06-realtime-and-ui-polish/06-VALIDATION.md
@.planning/phases/06-realtime-and-ui-polish/06-CONTEXT.md

<interfaces>
<!-- The locked tokens the polish pass may NOT alter (UI-SPEC) -->
- Accent: brand-red #C41230, reserved for: heading stripe, active pill bg, active variant
  chip, focus rings (focus-visible:ring-brand-red/50). Do NOT introduce a new accent.
- Color 60/30/10: bg-slate-50 page / bg-white card / brand-red accent. Destructive red-500.
- Typography: 2 weights (400/600), 3 sizes (12/14/24px). No heavier weights, no new sizes.
- Spacing: Tailwind default 4px scale; documented exceptions px-5/py-3, min-h-[44px]. No non-4px values.
- Depth: exactly 3 levels (L0 body / L1 bg-white card / L2 backdrop-blur glass). No 4th level.
- Layout: stacked cards <640px, table sm+, all 6 columns visible sm+. No column hiding on desktop.

Surface files in scope for polish (already built):
ArtifactTable.tsx, ArtifactTableRow.tsx, ArtifactCard.tsx, NewArtifactPill.tsx,
ArtifactSearchBar.tsx, VariantFilterBar.tsx, ArtifactEmptyState.tsx.

UI-SPEC §Accessibility Contract contrast pairs to validate manually:
slate-900/white ~21:1, slate-700/white ~9:1, slate-500/white ~4.6:1 (AA),
white/brand-red ~5.1:1 (AA), brand-red/white ~5.1:1 (AA). slate-400/white ~2.8:1 FAILS
(already upgraded to slate-500 in Plans 03-04).
</interfaces>
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 1: /frontend-design polish proposal — propose-then-approve (D-01/D-02)</name>
  <read_first>
    - .planning/phases/06-realtime-and-ui-polish/06-UI-SPEC.md (THE locked contract — every token; the polish may only operate within these)
    - .planning/phases/06-realtime-and-ui-polish/06-CONTEXT.md (D-01/D-02 authority boundary; §Deferred — conflicting ideas become v2)
    - portal-v2/src/components/artifacts/ArtifactTable.tsx, ArtifactTableRow.tsx, ArtifactCard.tsx, NewArtifactPill.tsx, ArtifactSearchBar.tsx, VariantFilterBar.tsx, ArtifactEmptyState.tsx (the surface being polished — current state after Plans 01-04)
  </read_first>
  <decision>Which /frontend-design polish refinements to land (all must fall WITHIN the locked UI-SPEC tokens), and which conflicting ideas to defer to v2.</decision>
  <context>
    Invoke the `/frontend-design` skill against the artifact-table + search/filter surface
    as an execution-polish layer ONLY (D-01): microinteractions, hover/focus-visible states,
    density/rhythm tuning, empty/loading/error-state refinement, transition timing within the
    locked Animation Contract. Write the full proposal to
    `.planning/phases/06-realtime-and-ui-polish/06-DESIGN-POLISH.md` with two sections:
    (A) "Proposed polish — within locked tokens" (each item cites the UI-SPEC token it
    operates within), and (B) "Deferred to v2 — conflicts with locked tokens" (any idea that
    would change accent identity, layout paradigm, typography weights/sizes, non-4px spacing,
    or add a 4th depth level — captured, NOT applied, per D-01). Present (A) and (B) to the
    operator for approval. Nothing lands before approval (D-02).
  </context>
  <action>
    1. Run `/frontend-design` over the seven surface files. Generate the proposal doc
       `06-DESIGN-POLISH.md` (sections A + B as above).
    2. PAUSE and present sections A + B to the operator. Do NOT edit any component yet.
    3. On approval, apply ONLY the approved section-A items, each verified against the
       UI-SPEC token it claims to operate within. Re-run `cd portal-v2 && npm run build &&
       npm test` after edits — must stay green (the polish must not break the jest-axe
       regression net or any test).
    4. Record the operator's approval (and any items they rejected) at the bottom of
       `06-DESIGN-POLISH.md` under "Approval".
    GUARDRAIL: if a proposed item cannot be mapped to a locked token, it is a section-B v2
    deferral — never apply it. Do not introduce a new accent color, a 4th depth level, a new
    type weight/size, or non-4px spacing.
  </action>
  <how-to-verify>
    1. `06-DESIGN-POLISH.md` exists with sections A (within-token proposals) and B (deferred-v2 conflicts).
    2. Only section-A approved items were applied; each cites a UI-SPEC token.
    3. `cd portal-v2 && npm run build && npm test` exits 0 after the edits.
    4. The "Approval" record is present.
  </how-to-verify>
  <acceptance_criteria>
    - `.planning/phases/06-realtime-and-ui-polish/06-DESIGN-POLISH.md` contains a "Proposed polish — within locked tokens" section and a "Deferred to v2" section.
    - No applied edit introduces a non-UI-SPEC token (grep the diff for new color hex/accent, new font-weight beyond 400/600, new text-size beyond text-xs/text-sm/text-2xl, or non-4px spacing — none present).
    - `06-DESIGN-POLISH.md` contains an "Approval" record.
    - `cd portal-v2 && npm run build && npm test` exits 0.
  </acceptance_criteria>
  <resume-signal>Type "approved" (optionally listing rejected items), or describe required changes.</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Manual WCAG-AA + Realtime UAT walkthrough (D-07 manual pass)</name>
  <read_first>
    - .planning/phases/06-realtime-and-ui-polish/06-VALIDATION.md (§"Manual-Only Verifications" — the exact manual checklist + why each is manual)
    - .planning/phases/06-realtime-and-ui-polish/06-UI-SPEC.md (§"Accessibility Contract" — keyboard nav table, ARIA roles, contrast pairs)
    - .planning/phases/06-realtime-and-ui-polish/06-RESEARCH.md (§"Manual UAT Boundary"; §"Common Pitfalls" Pitfall 5 jsdom color-contrast disabled)
    - .planning/phases/06-realtime-and-ui-polish/06-CONTEXT.md (D-07 — both passes; D-03/D-04 live Realtime behavior)
  </read_first>
  <what-built>
    The full Phase 06 surface is in place and tested: count-only Realtime toast + pill,
    responsive table↔card swap, opacity-only row stagger, jest-axe regression net. This
    checkpoint is the D-07 MANUAL pass that covers what CI cannot: real color-contrast
    (jsdom disables the axe rule), real keyboard/focus rings, screen-reader announcement,
    live Realtime timing, real-viewport responsive behavior, and OS reduced-motion. The
    agent first writes the checklist to `06-HUMAN-UAT.md`, then the operator performs it.
  </what-built>
  <how-to-verify>
    First, write `.planning/phases/06-realtime-and-ui-polish/06-HUMAN-UAT.md` containing the
    checklist below (one checkbox each). Then perform it against the deployed/dev portal
    (logged in as an admin/billing user on project poeyztlmsawfoqlanucc):
    1. LIVE REALTIME (DATA-06/D-03): with the portal open, trigger or await a CI billing run
       (or insert a test artifact row); confirm a "N new artifact(s)" info toast appears AND
       the "Load N" pill appears within a few seconds; click Load → table refetches and the
       pill clears; confirm no rows auto-inserted mid-scroll. Then dismiss a fresh pill (×) →
       it clears WITHOUT refetching.
    2. KEYBOARD NAV (UI-03): Tab order reaches search bar → variant chips (Enter/Space toggles,
       aria-pressed/checked) → sort headers (Enter/Space sorts, aria-sort updates) → download
       buttons → pill (Enter loads, Escape/× dismisses) → toast dismiss. Every focused element
       shows a visible brand-red focus ring.
    3. SCREEN READER (UI-03): with NVDA/VoiceOver, confirm the pill (role="status",
       aria-live="polite") is announced on INSERT; download button announces "Download {filename}".
    4. COLOR CONTRAST (UI-03): with the axe browser extension, validate the UI-SPEC contrast
       pairs (slate-500/white ≥4.5:1, white/brand-red ≥4.5:1, slate-700/white, slate-900/white);
       confirm no `text-slate-400` carrying meaning remains.
    5. RESPONSIVE (UI-01): at 375px → stacked ArtifactCard list with all 6 fields + 44px
       download target; at 768px and 1280px → virtualized table, all 6 columns visible.
    6. REDUCED MOTION (UI-02): DevTools "Emulate prefers-reduced-motion: reduce" → row entrance,
       pill, and card animations are eliminated/instant.
    Record PASS/FAIL + notes per item in `06-HUMAN-UAT.md`.
  </how-to-verify>
  <acceptance_criteria>
    - `.planning/phases/06-realtime-and-ui-polish/06-HUMAN-UAT.md` exists with all 6 checklist items (Live Realtime, Keyboard, Screen reader, Color-contrast, Responsive, Reduced-motion).
    - Each item has a recorded PASS/FAIL + notes.
    - Any FAIL is captured as a gap (for `/gsd-plan-phase --gaps`) rather than silently passed.
  </acceptance_criteria>
  <resume-signal>Type "approved" if all manual items pass, or list the failing items to capture as gaps.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Operator approval → applied UI edits | The propose-then-approve gate (D-02) is the control preventing unreviewed visual change |
| Browser → assistive tech | Keyboard/screen-reader interaction surface validated manually |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-14 | Tampering | /frontend-design silently overriding the approved UI-SPEC | mitigate | D-02 propose-then-approve gate: proposal written to 06-DESIGN-POLISH.md and presented BEFORE any edit; conflicting ideas deferred to v2 (section B); diff grepped for off-token values. |
| T-06-15 | Information Disclosure | Manual UAT performed against the live project | accept | UAT is performed by the authenticated operator (admin/billing) against poeyztlmsawfoqlanucc — the intended privileged user; no new exposure. Realtime remains count-only + RLS-gated (Plan 03). |
| T-06-16 | Repudiation | Accessibility regressions slipping past CI (jsdom color-contrast gap) | mitigate | D-07 dual pass: jest-axe regression net (Plans 01-04) for structure/ARIA + documented manual color-contrast/keyboard/screen-reader walkthrough recorded in 06-HUMAN-UAT.md. |
</threat_model>

<verification>
- `06-DESIGN-POLISH.md` exists with proposal (A) + deferred-v2 (B) + Approval record.
- Applied polish edits stay within locked tokens; `cd portal-v2 && npm run build && npm test` exits 0.
- `06-HUMAN-UAT.md` exists with all 6 manual items recorded PASS/FAIL.
</verification>

<success_criteria>
- D-01/D-02: /frontend-design ran as a propose-then-approve polish layer within locked tokens; conflicts deferred to v2; nothing landed unapproved.
- D-07: WCAG AA verified by both jest-axe (automated regression net) and the manual walkthrough; color-contrast covered manually (jsdom gap acknowledged).
- DATA-06 live behavior confirmed in UAT (toast + pill within seconds; no mid-scroll auto-insert).
</success_criteria>

<output>
After completion, create `.planning/phases/06-realtime-and-ui-polish/06-05-SUMMARY.md`.
</output>
