---
phase: 05-artifact-table-and-search
plan: 04
type: execute
wave: 4
depends_on: ["05-03"]
files_modified:
  - portal-v2/src/components/artifacts/ArtifactSearchBar.tsx
  - portal-v2/src/components/artifacts/VariantFilterBar.tsx
  - portal-v2/src/components/artifacts/ArtifactTable.tsx
  - portal-v2/src/components/artifacts/__tests__/VariantFilterBar.test.tsx
  - portal-v2/src/components/artifacts/__tests__/ArtifactSearchBar.test.tsx
autonomous: true
requirements: [SEARCH-01, SEARCH-02, SEARCH-03, SEARCH-04]
must_haves:
  truths:
    - "The search input debounces 250ms before it changes the query (SEARCH-01)"
    - "Search matches WR # OR week-ending via the server-side ilike .or() chain (SEARCH-01)"
    - "Variant filter is a multi-select with clearable chips using friendly labels (SEARCH-02 / D-10)"
    - "Variant options are derived dynamically from the variants present in the data (SEARCH-04)"
    - "Column headers toggle sort with asc/desc indicators; sort is server-side (SEARCH-03 / D-06)"
    - "Search + variant filter + sort combine — results satisfy all active constraints simultaneously (SEARCH-04)"
  artifacts:
    - path: "portal-v2/src/components/artifacts/ArtifactSearchBar.tsx"
      provides: "debounced controlled search input with a clear button"
      exports: ["ArtifactSearchBar"]
    - path: "portal-v2/src/components/artifacts/VariantFilterBar.tsx"
      provides: "multi-select variant control + clearable friendly-label chips"
      exports: ["VariantFilterBar"]
    - path: "portal-v2/src/components/artifacts/ArtifactTable.tsx"
      provides: "search/variants/sorting lifted into state and fed into useArtifactsInfinite params"
      contains: "useState"
  key_links:
    - from: "portal-v2/src/components/artifacts/ArtifactSearchBar.tsx"
      to: "useDebounce"
      via: "debounced value drives the params.search passed to useArtifactsInfinite"
      pattern: "useDebounce\\("
    - from: "portal-v2/src/components/artifacts/ArtifactTable.tsx"
      to: "useArtifactsInfinite params"
      via: "search + variants + sorting state -> ArtifactsQueryParams (queryKey re-fetch)"
      pattern: "sortColumn|sortAscending"
    - from: "portal-v2/src/components/artifacts/VariantFilterBar.tsx"
      to: "getVariantLabel"
      via: "render dynamic variant options + chips through the friendly-label map"
      pattern: "getVariantLabel\\("
---

<objective>
Wire the interactive search, variant filter, and column sort onto the table so all
three combine server-side (SEARCH-04). Add a debounced (250ms) search input that
filters by WR # OR week-ending (SEARCH-01), a multi-select variant filter with
clearable friendly-label chips whose options are derived dynamically from the data
(SEARCH-02 / SEARCH-04 / D-10), and interactive sortable column headers with asc/desc
indicators (SEARCH-03). Lift `search`, `variants`, and `sorting` into `ArtifactTable`
state and feed them into `useArtifactsInfinite`'s `params`; because those values are
in the `queryKey`, every change re-fetches from page 0 with the new
`.or()/.in()/.order()` applied — combining cleanly with the existing infinite scroll.

Purpose: Complete the search/filter/sort surface (SEARCH-01..04) on the virtualized
real-data table from Plan 03.
Output: ArtifactSearchBar, VariantFilterBar, updated ArtifactTable wiring, and tests
for the debounce + dynamic-options + chip-clear behaviors.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-artifact-table-and-search/05-CONTEXT.md
@.planning/phases/05-artifact-table-and-search/05-RESEARCH.md
@.planning/phases/05-artifact-table-and-search/05-PATTERNS.md
@.planning/phases/05-artifact-table-and-search/05-VALIDATION.md
@.planning/phases/05-artifact-table-and-search/05-03-SUMMARY.md

<interfaces>
<!-- From Plan 01: -->
import { useDebounce } from '../../hooks/useDebounce';        // 250ms
import { getVariantLabel } from '../../lib/variantLabels';

<!-- From Plan 02: -->
import { useArtifactsInfinite, type ArtifactsQueryParams } from '../../hooks/useArtifactsInfinite';
//   ArtifactsQueryParams = { search: string; variants: string[];
//     sortColumn: 'week_ending'|'work_request'|'size_bytes'|'created_at'; sortAscending: boolean }

<!-- From Plan 03: ArtifactTable already holds the table + virtualizer + 4 states with FIXED params.
     This plan replaces the fixed `params` object with lifted state and renders the controls above the table. -->

<!-- TanStack Table sort state (installed Plan 01): -->
import { type SortingState } from '@tanstack/react-table';
//   sorting[0] = { id: <columnId>, desc: boolean }  -> map to sortColumn + sortAscending

<!-- Existing primitives: Badge (chip shell), lucide-react X (clear), SearchBar.tsx analog,
     ArrowUp/ArrowDown/ArrowUpDown lucide icons for sort indicators. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: ArtifactSearchBar (debounced) + VariantFilterBar (dynamic multi-select + clearable chips)</name>
  <files>portal-v2/src/components/artifacts/ArtifactSearchBar.tsx, portal-v2/src/components/artifacts/VariantFilterBar.tsx, portal-v2/src/components/artifacts/__tests__/VariantFilterBar.test.tsx, portal-v2/src/components/artifacts/__tests__/ArtifactSearchBar.test.tsx</files>
  <read_first>
    - portal-v2/src/components/dashboard/SearchBar.tsx (exact analog — controlled input styling + prop shape)
    - portal-v2/src/components/ui/Badge.tsx (chip shell) + Toast.tsx lines 61-65 (lucide X clear button)
    - portal-v2/src/hooks/useDebounce.ts (Plan 01) + portal-v2/src/lib/variantLabels.ts (Plan 01)
    - 05-PATTERNS.md "ArtifactSearchBar.tsx" (lines 332-348), "VariantFilterBar.tsx" (lines 352-372)
    - 05-CONTEXT.md D-09 (250ms debounce, WR#+week-ending scope), D-10 (friendly labels, dynamic options, clearable chips)
  </read_first>
  <action>
    `portal-v2/src/components/artifacts/ArtifactSearchBar.tsx` — a controlled input modeled on
    `SearchBar.tsx`. Props: `{ value: string; onChange: (v: string) => void; placeholder?: string }`.
    Use the existing SearchBar Tailwind styling + a leading search icon (lucide `Search`) and a
    trailing clear (X) button (lucide `X`, shown only when `value` is non-empty) that calls
    `onChange('')`. Placeholder: "Search by WR # or week-ending (e.g. 90001 or 05/26/25)".
    The DEBOUNCE itself lives in the parent (ArtifactTable, Task 2) via `useDebounce` — this
    component is a plain controlled input so the user sees their keystrokes immediately while the
    query debounces. (Do not debounce the raw input value; debounce the value handed to the query.)

    `portal-v2/src/components/artifacts/VariantFilterBar.tsx` — multi-select + chips. Props:
    `{ options: string[]; selected: string[]; onChange: (next: string[]) => void }`.
    - `options` are the dynamic distinct variant strings present in the data (passed down from the
      table — SEARCH-04). Render each option through `getVariantLabel(option)` (D-10 friendly
      label); the underlying value stays the raw variant string.
    - No multi-select component exists (PATTERNS line 371) — keep it minimal: a `<details>/<summary>`
      popover OR an inline row of toggle chips. Each option is a clickable `Badge`-styled chip;
      clicking toggles membership in `selected` (call `onChange`).
    - Selected variants render as clearable chips: `Badge` shell + an inner `<button>` with
      `<X size={10} />` that removes that variant from `selected`. A "Clear all" affordance when
      `selected.length > 0`.

    Tests:
    - `ArtifactSearchBar.test.tsx`: typing fires `onChange` with the typed value; the clear (X)
      button appears when value is non-empty and calls `onChange('')`.
    - `VariantFilterBar.test.tsx`: given `options: ['', 'helper', '_AEPBillable']`, the rendered
      option labels include "Primary", "Helper", "AEP Billable (Sub)" (getVariantLabel applied);
      clicking an option calls `onChange` with that raw variant added; clicking a selected chip's
      X calls `onChange` with it removed.
  </action>
  <verify>
    <automated>cd portal-v2 && npm test -- src/components/artifacts/__tests__/VariantFilterBar.test.tsx src/components/artifacts/__tests__/ArtifactSearchBar.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "getVariantLabel" portal-v2/src/components/artifacts/VariantFilterBar.tsx` returns 1 (friendly labels, D-10)
    - `grep -Ec "X\\b" portal-v2/src/components/artifacts/VariantFilterBar.tsx` >= 1 (clearable chip X) AND a "Clear" affordance present
    - `grep -c "onChange('')" portal-v2/src/components/artifacts/ArtifactSearchBar.tsx` returns 1 (clear button)
    - `grep -c "useDebounce" portal-v2/src/components/artifacts/ArtifactSearchBar.tsx` returns 0 (debounce lives in the parent, not the input)
    - `cd portal-v2 && npm test` exits 0
  </acceptance_criteria>
  <done>Search input (controlled + clear) and variant multi-select with dynamic friendly-label options + clearable chips exist; component tests green.</done>
</task>

<task type="auto">
  <name>Task 2: Wire search + variants + sort into ArtifactTable (combine server-side, SEARCH-04)</name>
  <files>portal-v2/src/components/artifacts/ArtifactTable.tsx</files>
  <read_first>
    - portal-v2/src/components/artifacts/ArtifactTable.tsx (Plan 03 output — has fixed params + table + virtualizer + 4 states)
    - portal-v2/src/components/artifacts/ArtifactSearchBar.tsx + VariantFilterBar.tsx (Task 1)
    - portal-v2/src/hooks/useDebounce.ts (Plan 01)
    - 05-RESEARCH.md Pattern 4 (manualSorting + derive sortColumn/sortAscending from sorting[0], lines 403-442), Open Question 1 (dynamic variant options, lines 784-787)
    - 05-CONTEXT.md D-06 (default week_ending DESC), D-09 (debounce + server-side combine), SEARCH-04
  </read_first>
  <action>
    Replace the fixed `params` object from Plan 03 with lifted state and render the controls above
    the table:

    1. State:
       ```typescript
       const [searchInput, setSearchInput] = useState('');
       const debouncedSearch = useDebounce(searchInput, 250);   // SEARCH-01 / D-09
       const [variants, setVariants] = useState<string[]>([]);
       const [sorting, setSorting] = useState<SortingState>([{ id: 'week_ending', desc: true }]); // D-06
       ```
    2. Derive params from state (sortColumn/sortAscending from `sorting[0]`; fall back to the
       week_ending DESC default if sorting is empty):
       ```typescript
       const sort = sorting[0] ?? { id: 'week_ending', desc: true };
       const params: ArtifactsQueryParams = {
         search: debouncedSearch,
         variants,
         sortColumn: sort.id as ArtifactsQueryParams['sortColumn'],
         sortAscending: !sort.desc,
       };
       ```
       Because `params` flows into `useArtifactsInfinite`'s `queryKey`, any change re-fetches from
       page 0 with the new `.or()/.in()/.order()` — search + variant + sort combine automatically
       (SEARCH-04). No client-side filtering/sorting (`manualSorting`/`manualFiltering` already true).
    3. Wire `useReactTable`'s `onSortingChange: setSorting` (Plan 03 set the column defs +
       manual flags; this plan connects interactive header clicks). In each sortable column header,
       render an asc/desc indicator with lucide icons (`ArrowUp` when `sort.id===col && !sort.desc`,
       `ArrowDown` when `sort.id===col && sort.desc`, neutral `ArrowUpDown` otherwise) and an
       onClick that toggles via TanStack Table's `header.column.getToggleSortingHandler()` (or a
       manual `setSorting` that flips desc / switches column). `variant` and `download` columns
       stay non-sortable (`enableSorting: false`).
    4. Dynamic variant options (SEARCH-04 / D-10 / Open Question 1): derive from a dedicated
       lightweight query of distinct variants so options reflect the FULL dataset, not just the
       current page/filter. Add a small `useQuery(['artifact-variants'])` that runs
       `supabase.from('artifacts').select('variant')` and dedupes
       (`Array.from(new Set(rows.map(r => r.variant)))`), OR derive from `allRows` as an interim
       (`Array.from(new Set(allRows.map(r => r.variant)))`) — prefer the dedicated query so a
       narrow filter doesn't hide other variants. Pass options into `<VariantFilterBar>`.
    5. Render the controls row above the table card: `<ArtifactSearchBar value={searchInput}
       onChange={setSearchInput} />` and `<VariantFilterBar options={variantOptions}
       selected={variants} onChange={setVariants} />`.
    6. Update `NoResultsState`'s `onClear` to reset all three:
       `() => { setSearchInput(''); setVariants([]); }` (sort can stay at default).
    7. Update the empty-vs-no-results branch to use `debouncedSearch` (not raw input):
       empty-DB requires `!debouncedSearch && variants.length === 0`.
  </action>
  <verify>
    <automated>cd portal-v2 && npm test && npm run build && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "useDebounce(searchInput, 250)" portal-v2/src/components/artifacts/ArtifactTable.tsx` returns 1 (SEARCH-01 / D-09)
    - `grep -c "onSortingChange: setSorting" portal-v2/src/components/artifacts/ArtifactTable.tsx` returns 1 (SEARCH-03)
    - `grep -c "<ArtifactSearchBar" portal-v2/src/components/artifacts/ArtifactTable.tsx` returns 1 AND `grep -c "<VariantFilterBar" ...` returns 1
    - `grep -Ec "sortColumn|sortAscending" portal-v2/src/components/artifacts/ArtifactTable.tsx` >= 1 (sorting -> params mapping)
    - `grep -c "setVariants(\\[\\])" portal-v2/src/components/artifacts/ArtifactTable.tsx` >= 1 (Clear filters resets variants)
    - `cd portal-v2 && npm test` exits 0 ; `npm run build` exits 0 ; `npm run lint` exits 0
  </acceptance_criteria>
  <done>Debounced search, dynamic variant multi-select, and interactive column sort are wired into useArtifactsInfinite params and combine server-side (SEARCH-04); Clear filters resets search+variants; full suite + build + lint green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user search/filter/sort input → PostgREST query | Search text, variant set, and sort column all become server query parameters |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-11 | Tampering | search term → `.or()` (PostgREST injection) | mitigate | Sanitize+normalize already enforced inside `useArtifactsInfinite` (Plan 02); this plan only feeds the raw debounced string in — the hook is the single sanitization seam. |
| T-05-12 | Tampering | sort column injection via `sortColumn` | mitigate | `sortColumn` is typed to the 4-value union (`'week_ending'|'work_request'|'size_bytes'|'created_at'`) and sourced from fixed column defs — no free-text column name reaches `.order()`. |
| T-05-13 | Tampering | variant filter values | mitigate | Variant options derive from `SELECT variant` (RLS-scoped) values; `.in('variant', [...])` parameterizes the list — no raw interpolation. |
</threat_model>

<verification>
- `cd portal-v2 && npm test` — search bar + variant filter component tests green
- `cd portal-v2 && npm run build` — sorting→params typing + dynamic options compile
- `cd portal-v2 && npm run lint` — clean
- Manual (phase gate, 05-VALIDATION.md): type a partial WR# → table narrows after 250ms; select a variant → results combine with search; click a header → server re-sorts; clear filters → full set returns
</verification>

<success_criteria>
- Search input debounces 250ms before changing the query; matches WR # OR week-ending server-side (SEARCH-01)
- Variant multi-select with clearable friendly-label chips; options derived dynamically from data (SEARCH-02 / SEARCH-04 / D-10)
- Sortable column headers with asc/desc indicators; sort is server-side (SEARCH-03 / D-06)
- Search + variants + sort combine — results satisfy all active constraints simultaneously (SEARCH-04)
- Clear filters resets search + variants
- npm test + build + lint green
</success_criteria>

<output>
After completion, create `.planning/phases/05-artifact-table-and-search/05-04-SUMMARY.md`
</output>
