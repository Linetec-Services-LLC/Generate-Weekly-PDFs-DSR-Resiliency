---
phase: quick-260528-lu6
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [AGENTS.md]
autonomous: true
requirements: [LU6-01, LU6-02, LU6-03, LU6-04]

must_haves:
  truths:
    - "AGENTS.md is lean (~340-360 lines, same order of magnitude as the 346-line CLAUDE.md), not ~2,200 lines"
    - "AGENTS.md contains NO inlined Living Ledger entries — it points to memory-bank/living-ledger.md instead"
    - "AGENTS.md contains NO .Codex/ path references; all rule/command paths point to .claude/"
    - "AGENTS.md's autonomous-memory section instructs appending future entries to memory-bank/living-ledger.md, NOT to AGENTS.md's own bottom"
    - "AGENTS.md remains self-sufficient for Codex tooling: retains Project Summary, Tech Stack, Repository Layout, Build/Test/Run, Data Pipeline Architecture, Config env vars, advanced_options parser, Smartsheet standards, Conventions, Critical Pitfalls, Detailed References"
    - "AGENTS.md is Codex-flavored: header references Codex (not Claude Code), @claude trigger becomes @Codex/AGENTS-appropriate wording"
    - "No production code, tests, workflows, portal, portal-v2, website, or scripts files are modified — AGENTS.md is the only changed file"
  artifacts:
    - path: "AGENTS.md"
      provides: "Lean Codex-flavored mirror of CLAUDE.md with a Living Ledger pointer section"
      min_lines: 330
      contains: "memory-bank/living-ledger.md"
  key_links:
    - from: "AGENTS.md"
      to: "memory-bank/living-ledger.md"
      via: "Living Ledger pointer section + autonomous-memory append instruction"
      pattern: "memory-bank/living-ledger\\.md"
---

<objective>
Replace the bloated, untracked AGENTS.md (~2,207 lines) with a lean Codex-flavored mirror of the current canonical CLAUDE.md (346 lines). The bloated AGENTS.md re-inlines a stale Living Ledger (29 entries, last 2026-05-20), references a non-existent `.Codex/` layout, and reinstates the anti-pattern of appending future ledger entries to its own bottom — directly undoing commit a3e0151's lean-up.

Purpose: Restore documentation consistency so Codex/OpenAI tooling reads the same lean operational guidance Claude reads, deferring the Living Ledger to the single canonical source (`memory-bank/living-ledger.md`, 47 entries through 2026-05-28).

Output: A single rewritten `AGENTS.md` at the repo root — Codex-flavored, lean, ledger-pointing. No other file changes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.claude/rules/documentation-maintenance.md

<reference_notes>
- CLAUDE.md (346 lines) is the canonical lean template. Mirror its section order EXACTLY:
  1. Title + one-line "guidance to ___" intro
  2. ## Project Summary — Billing Automation & Excel Generation (Project Overview, Tech Stack & Constraints, Architecture Decisions, Development Conventions, Boundaries & Guardrails, Validation Commands)
  3. ## ⚠️ AUTONOMOUS CLOUD MEMORY INJECTION (CRITICAL)
  4. ## Role & Persona ("God-Mode")
  5. ## Production Safety & Code Modification
  6. ## Repository Layout (3 Coupled Components)
  7. ## Build, Test, and Run Commands (Python core / Portal / Portal-v2 / Docusaurus)
  8. ## Data Pipeline Architecture (Python core)
  9. ## Configuration — 30+ Environment Variables
  10. ## GitHub Actions Workflow — `advanced_options` Parser
  11. ## Smartsheet API & Integration Standards
  12. ## Current Stack & Ecosystem Context
  13. ## Conventions (Language-Specific)
  14. ## Architectural Consultant & Language Selection
  15. ## Multi-Disciplinary Best Practices
  16. ## GitHub Cloud Action & PR Standards
  17. ## Critical Pitfalls (Known Footguns)
  18. ## Detailed References
  19. ## Living Ledger (Auto-Updated Context)  ← POINTER ONLY

- Codex-flavor substitutions (and ONLY these — keep all technical content byte-identical otherwise):
  * Header line "This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository." → "This file provides guidance to Codex (OpenAI Codex / agentic coding agents) when working with code in this repository."
  * In AUTONOMOUS CLOUD MEMORY INJECTION: "When triggered via `@claude` in a GitHub issue or CLI" → "When triggered via `@Codex` in a GitHub issue or CLI". Keep the rest of that sentence and the "append to memory-bank/living-ledger.md (NOT this AGENTS.md file — keep AGENTS.md lean)" instruction. Note: CLAUDE.md self-refers as "this `CLAUDE.md` file"; the AGENTS.md equivalent must self-refer as "this `AGENTS.md` file".
  * `.github/hooks/pre-push-tests.json` paragraph in CLAUDE.md says it is "a **Claude Code hook**". For AGENTS.md keep the factual description accurate: it is a Claude Code hook (that is what the file actually is) — so state it as: "`.github/hooks/pre-push-tests.json` is a Claude Code hook (not a standard Git `pre-push` hook) and does not gate Codex/terminal pushes — run `pytest tests/` manually before pushing." Do NOT rename the hook or pretend Codex consumes it.
  * Detailed References: keep `.github/...` paths verbatim (these are real). The one pointer that must mention CLAUDE.md vs AGENTS.md is the Living Ledger section — see below.

- Living Ledger (Auto-Updated Context) section — adapt CLAUDE.md's blockquote pointer (lines 328-346) to AGENTS.md:
  * Keep the same 3-paragraph blockquote structure pointing to memory-bank/living-ledger.md.
  * Replace "moved out of `CLAUDE.md` on 2026-05-28" framing with AGENTS.md-appropriate wording: e.g. "AGENTS.md defers the full Living Ledger to `memory-bank/living-ledger.md` (47+ dated entries) to stay lean — mirroring CLAUDE.md."
  * Keep the "Self-documenting: append new dated `[YYYY-MM-DD HH:MM]` entries to the BOTTOM of `memory-bank/living-ledger.md` — do NOT inline the ledger into this file." instruction.
  * Do NOT copy any actual ledger entries.

- Path fixes: there are NO valid `.Codex/` paths in this repo. The real rules live in `.claude/rules/` (confirmed: documentation-maintenance.md, smartsheet-python-optimization.md). The bloated AGENTS.md referenced `.Codex/rules/` and `.Codex/commands/`. Since the lean CLAUDE.md template does NOT reference `.claude/rules/` or `.claude/commands/` in its body at all, the clean mirror simply will not contain any `.Codex/` reference — do not invent a `.claude/rules/` reference where CLAUDE.md has none. The audit just confirms zero `.Codex/` strings remain.

- AGENTS.md is currently untracked; the executor overwrites it entirely via the Write tool (single atomic rewrite is cleaner than surgical edits on 2,207 lines).
</reference_notes>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite AGENTS.md as a lean Codex-flavored mirror of CLAUDE.md</name>
  <files>AGENTS.md</files>
  <action>
Overwrite AGENTS.md entirely using the Write tool (do NOT attempt surgical Edits on the 2,207-line bloated file — a full rewrite is the atomic fix).

Produce a Codex-flavored mirror of the canonical CLAUDE.md (@CLAUDE.md, 346 lines). Copy ALL 19 sections in the EXACT order listed in `<reference_notes>`, with technical content byte-identical to CLAUDE.md EXCEPT for the Codex-flavor substitutions enumerated in `<reference_notes>`:

LU6-01 (re-inlined ledger removed): The final section is `## Living Ledger (Auto-Updated Context)` containing ONLY the adapted 3-paragraph blockquote pointer to memory-bank/living-ledger.md. ZERO inlined ledger entries.

LU6-02 (staleness eliminated): No ledger entries are copied at all — AGENTS.md defers entirely to the canonical memory-bank/living-ledger.md (47 entries). The pointer wording references "47+ dated entries" living in memory-bank/living-ledger.md.

LU6-03 (broken self-reference fixed): No `.Codex/` strings anywhere. Keep real `.github/...` paths verbatim from CLAUDE.md's Detailed References. Do not invent `.claude/rules/` references CLAUDE.md does not have.

LU6-04 (anti-pattern removed): The `## ⚠️ AUTONOMOUS CLOUD MEMORY INJECTION (CRITICAL)` section and the Living Ledger pointer both instruct appending future dated `[YYYY-MM-DD HH:MM]` entries to `memory-bank/living-ledger.md`, explicitly NOT to AGENTS.md's own bottom (keep AGENTS.md lean). Self-references in those sections say "this `AGENTS.md` file" not "this `CLAUDE.md` file".

Codex-flavor substitutions (apply ONLY these, per `<reference_notes>`):
- Title stays `# AGENTS.md`.
- Intro line: "This file provides guidance to Codex (OpenAI Codex / agentic coding agents) when working with code in this repository."
- `@claude` trigger → `@Codex` in the AUTONOMOUS CLOUD MEMORY INJECTION section.
- pre-push hook paragraph: state it is a Claude Code hook that does NOT gate Codex/terminal pushes; instruct manual `pytest tests/`.
- Living Ledger pointer: AGENTS.md-appropriate wording mirroring CLAUDE.md's deferral.

DOCUMENTATION-ONLY: AGENTS.md is the ONLY file written. Do not touch generate_weekly_pdfs.py, audit_billing_changes.py, tests/, .github/workflows/, portal/, portal-v2/, website/, scripts/, or memory-bank/living-ledger.md.

Target ~330-360 lines (same order of magnitude as CLAUDE.md's 346). It must read as self-sufficient operational guidance for a Codex agent that has never seen CLAUDE.md.
  </action>
  <verify>
    <automated>powershell -NoProfile -Command "$c = Get-Content AGENTS.md -Raw; $lines = (Get-Content AGENTS.md | Measure-Object -Line).Lines; $codex = ([regex]::Matches($c, '\.Codex/')).Count; $ledgerRefs = ([regex]::Matches($c, 'memory-bank/living-ledger\.md')).Count; $dated = ([regex]::Matches($c, '\[202[0-9]-[01][0-9]-[0-3][0-9] [0-2][0-9]:[0-5][0-9]\]')).Count; $hasInjection = if ($c -match 'AUTONOMOUS CLOUD MEMORY INJECTION') {1} else {0}; $hasLedgerSection = if ($c -match 'Living Ledger \(Auto-Updated Context\)') {1} else {0}; $codexTrigger = if ($c -match '@Codex') {1} else {0}; Write-Output ('lines=' + $lines); Write-Output ('dotCodexPaths=' + $codex + ' (must be 0)'); Write-Output ('ledgerPointerRefs=' + $ledgerRefs + ' (must be >=2)'); Write-Output ('inlinedDatedEntries=' + $dated + ' (must be 0)'); Write-Output ('hasInjectionSection=' + $hasInjection + ' (must be 1)'); Write-Output ('hasLedgerPointerSection=' + $hasLedgerSection + ' (must be 1)'); Write-Output ('hasCodexTrigger=' + $codexTrigger + ' (must be 1)'); $ok = ($codex -eq 0) -and ($ledgerRefs -ge 2) -and ($dated -eq 0) -and ($hasInjection -eq 1) -and ($hasLedgerSection -eq 1) -and ($codexTrigger -eq 1) -and ($lines -ge 330) -and ($lines -le 420); if ($ok) { Write-Output 'PASS' } else { Write-Output 'FAIL'; exit 1 }"</automated>
  </verify>
  <done>
AGENTS.md is 330-420 lines, contains zero `.Codex/` paths, contains zero inlined dated ledger entries, references memory-bank/living-ledger.md at least twice (injection section + ledger pointer section), has the AUTONOMOUS CLOUD MEMORY INJECTION section and the Living Ledger pointer section, and uses the @Codex trigger. Verify script prints PASS.
  </done>
</task>

<task type="auto">
  <name>Task 2: Confirm documentation-only blast radius (no other file touched)</name>
  <files>AGENTS.md</files>
  <action>
Confirm AGENTS.md is the only file changed in the working tree (modulo the untracked .planning/ quick artifacts). This is a guard that no production code, test, workflow, or portal file was incidentally modified.

Run the verify command. If anything other than AGENTS.md (and .planning/ planning artifacts) shows as modified/added in the diff against HEAD, STOP and report — the change must be documentation-only per the spec.
  </action>
  <verify>
    <automated>powershell -NoProfile -Command "$changed = git status --porcelain | ForEach-Object { $_.Substring(3) } | Where-Object { $_ -notmatch '^\.planning/' }; $bad = $changed | Where-Object { $_ -ne 'AGENTS.md' -and $_ -ne 'AGENTS.md\"' }; Write-Output '--- changed (excluding .planning/) ---'; $changed | ForEach-Object { Write-Output $_ }; if ($bad) { Write-Output 'FAIL: non-AGENTS.md file changed'; exit 1 } else { Write-Output 'PASS: only AGENTS.md changed' }"</automated>
  </verify>
  <done>
git status (excluding .planning/ artifacts) shows AGENTS.md as the only modified/untracked file. No production code, tests, workflows, portal, portal-v2, website, scripts, or memory-bank files appear. Verify script prints PASS.
  </done>
</task>

</tasks>

<verification>
- AGENTS.md line count is in the 330-420 range (same order of magnitude as CLAUDE.md's 346 lines).
- `grep -c '\.Codex/' AGENTS.md` (or PowerShell equivalent) returns 0.
- No inlined dated `[YYYY-MM-DD HH:MM]` ledger entries exist in AGENTS.md.
- The Living Ledger pointer section and the AUTONOMOUS CLOUD MEMORY INJECTION section both reference memory-bank/living-ledger.md.
- Section order and technical content match CLAUDE.md (spot-check Repository Layout, Data Pipeline Architecture, Config env vars, advanced_options parser, Critical Pitfalls).
- git status confirms AGENTS.md is the only non-.planning file changed.
</verification>

<success_criteria>
- AGENTS.md is a lean (~340 line) Codex-flavored mirror of CLAUDE.md, self-sufficient for Codex tooling.
- All four spec problems are fixed: (1) ledger de-inlined to a pointer, (2) staleness eliminated by deferring to canonical ledger, (3) all `.Codex/` references removed, (4) append-to-own-bottom anti-pattern replaced with append-to-living-ledger instruction.
- Documentation-only: AGENTS.md is the sole modified file.
</success_criteria>

<output>
After completion, create `.planning/quick/260528-lu6-reconcile-agents-md-into-a-lean-pointer-/260528-lu6-SUMMARY.md`
</output>
