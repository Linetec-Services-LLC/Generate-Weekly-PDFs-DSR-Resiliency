---
phase: quick-260603-mmc
plan: 02
type: execute
wave: 2
depends_on: [260603-mmc-01]
files_modified:
  - generate_weekly_pdfs.py
  - tests/test_subcontractor_pricing.py
  - requirements.txt
  - memory-bank/living-ledger.md
autonomous: true
requirements:
  - QUICK-SENTRY-DEFERRED-UPGRADES
must_haves:
  truths:
    - "The root Sentry transaction (_txn) carries final run-level KPIs (files/groups/uploaded/errored/duration/throughput) on the SUCCESS path, derived from a pure, unit-tested helper - counts only, no PII."
    - "On session failure, a PII-safe run-context snapshot (counts/booleans/redacted only) is attached to the Sentry error event via scope.add_attachment, built by a pure helper and wrapped so it never masks the original exception."
    - "A guarded structured-log helper exists (sentry_sdk.logger, SDK 2.54+) used at non-PII milestones only; it no-ops safely when the logger attribute is absent and emits nothing to Sentry unless SENTRY_ENABLE_LOGS is on. It NEVER receives row data."
    - "requirements.txt sentry-sdk floor is raised to >=2.54.0 (still 2.x, no 3.x)."
    - "pytest tests/ passes in full; python -m py_compile generate_weekly_pdfs.py succeeds."
  artifacts:
    - path: "generate_weekly_pdfs.py"
      provides: "Pure KPI + snapshot helpers; _txn.set_data run KPIs; failure-path add_attachment; guarded _sentry_log_event helper + milestone calls"
      contains: "add_attachment"
    - path: "tests/test_subcontractor_pricing.py"
      provides: "Unit tests for the pure KPI/snapshot/log helpers asserting structure + PII-safety (no WR list, names, prices)"
      contains: "kpi"
    - path: "requirements.txt"
      provides: "sentry-sdk>=2.54.0 floor"
      contains: "sentry-sdk>=2.54.0"
  key_links:
    - from: "main() success path (~:10206 _txn.set_status('ok'))"
      to: "root transaction run KPIs"
      via: "if _txn: for k,v in _build_run_kpis(...).items(): _txn.set_data(k, v)"
      pattern: "_build_run_kpis"
    - from: "main() except Exception (~:10231)"
      to: "PII-safe error attachment"
      via: "scope.add_attachment(bytes=json(_build_run_context_snapshot(...)), filename='run-context.json')"
      pattern: "add_attachment"
---

<objective>
Implement the three deferred Sentry upgrades from 260603-mmc-RESEARCH.md section 4
(#5 attachments, #6 root-transaction run KPIs, #7 structured logger) - additively,
PII-safely, and TDD-driven via pure helper functions. This deepens Sentry telemetry
(queryable run KPIs on the root transaction, downloadable run-context on failures,
optional structured logs) WITHOUT touching the production billing path.

NOTE on #6: the codebase ALREADY uses span.set_data pervasively (dozens of calls)
and the root _txn gets test_mode/github_actions at init. The gap this closes is the
FINAL aggregate run KPIs on the ROOT transaction, added on the success path.
</objective>

<context>
@.planning/quick/260603-mmc-fix-missing-old-rates-csv-default-fileno/260603-mmc-RESEARCH.md
@CLAUDE.md

<verified_interfaces note="orchestrator-traced 2026-06-03, line numbers approximate">
generate_weekly_pdfs.py:
- _redact_exception_message(exc, *, max_len=240) -> str            # :937  (PII scrubber)
- sentry_add_breadcrumb(category, message, level="info", data=None) # :972  (no-ops w/o DSN)
- Root transaction: _txn = sentry_sdk.start_transaction(...) at ~:8028; _txn.set_data("test_mode"...) at ~:8034.
- SUCCESS finish: `if _txn: _txn.set_status("ok"); _txn.__exit__(None,None,None)` at ~:10206-10209.
  Just ABOVE it (~:10198) a "Session completed successfully" breadcrumb already carries
  files_generated/duration/skipped/errored. Counts in scope here: generated_files_count,
  len(groups), _groups_skipped, _groups_generated, _groups_uploaded, _groups_errored,
  session_duration (a timedelta), _api_calls_count, plus 'source_sheets'/'all_rows' via dir() guard.
- _run_summary dict (counts only, PII-safe) built at ~:10070-:10091 in the success block.
- FAILURE handler: `except Exception as e:` at ~:10231; inside `if SENTRY_DSN:` it gets
  scope = sentry_sdk.get_isolation_scope(), sets tags, then sentry_capture_with_context(...)
  with fingerprint=["session-failure", type(e).__name__]. `e`, session_duration, _groups_*,
  groups (via dir() guard) are in scope. ADD the attachment to `scope` BEFORE the capture call.
- requirements.txt line 2: `sentry-sdk>=2.35.0`  -> bump to `>=2.54.0`.
- SENTRY_ENABLE_LOGS parser: _parse_sentry_enable_logs(raw) at ~:1268 (returns bool). Default OFF.
</verified_interfaces>

<guardrails>
- Additive/surgical. Do NOT touch the billing/grouping/hashing/filename/upload paths,
  _sanitize_csv_path, the rate loaders, or the cron monitor_config (done in plan 01).
- PII (HARD): every value reaching Sentry via set_data / add_attachment / logger attributes
  must be a COUNT, BOOLEAN, fixed enum, static string, or _redact_exception_message output.
  NEVER WR numbers, foreman/dept/job names, customer names, dollar amounts, or raw rows.
  The attachment and the structured logger BOTH bypass before_send_log - this is why the
  snapshot/log helpers must be counts-only and unit-tested for it.
- Every new Sentry call must be defensive: wrap add_attachment and logger calls in try/except
  so they NEVER mask the original error or fail the run. _txn.set_data is already safe but
  still guard with `if _txn:`.
- Do NOT add set_measurement (deprecated 2.28.0). Use _txn.set_data.
- Stay on sentry-sdk 2.x. No 3.x APIs (set_attribute/OTel). max floor 2.54.0.
</guardrails>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: TDD - pure PII-safe helpers (KPIs, snapshot, structured-log wrapper)</name>
  <files>tests/test_subcontractor_pricing.py, generate_weekly_pdfs.py</files>
  <action>
  RED first: in tests/test_subcontractor_pricing.py add a new test class (e.g.
  TestSentryTelemetryHelpers) with tests for THREE new pure helpers you will add to
  generate_weekly_pdfs.py. Write the tests first; confirm they fail (helpers absent).

  Helper A - _build_run_kpis(*, files_generated, groups_total, groups_skipped,
  groups_generated, groups_uploaded, groups_errored, duration_seconds,
  sheets_discovered, rows_fetched, api_calls) -> dict[str, int|float]:
    Returns a flat dict of numeric KPIs INCLUDING a derived throughput, e.g.
    "groups_per_minute": round(groups_generated / (duration_seconds/60), 2) if
    duration_seconds else 0.0. ALL values numeric. No strings that could carry PII.
    Tests: returns expected keys; throughput computed; zero-duration -> 0.0 (no ZeroDivisionError);
    every value is int or float (assert no str values -> guarantees no PII leakage).

  Helper B - _build_run_context_snapshot(*, success: bool, duration_seconds: float,
  groups_attempted, groups_generated, groups_uploaded, groups_errored,
  error_type: str|None=None) -> dict:
    Returns counts/booleans + (if error_type) the already-safe type name only.
    Tests: success/failure shapes; values are only int/float/bool/None or the passed
    error_type string; assert NO key/value contains a WR-like token, a name, or a '$'.
    (Build a sample call with deliberately-safe inputs and assert the serialized JSON
    contains none of: a WR number pattern, a dollar sign, a sample foreman name.)

  Helper C - _sentry_log_event(level: str, message: str, **attributes) -> None:
    Guarded structured-log emitter. MUST:
      - return immediately if not SENTRY_DSN
      - return immediately if not hasattr(sentry_sdk, "logger") (older SDK safety)
      - call getattr(sentry_sdk.logger, level, sentry_sdk.logger.info)(message, **attributes)
        inside try/except (swallow + logging.debug on failure; never raise)
    Add a module docstring/comment: "ONLY pass non-PII scalars (counts, booleans, fixed
    enums). This path BYPASSES before_send_log - never pass row data."
    Tests: no DSN -> no-op (monkeypatch SENTRY_DSN to '' / None and assert it returns
    without raising); when sentry_sdk has no 'logger' attr (monkeypatch/delattr in a
    context) it no-ops; it never raises on a bad level.

  GREEN: implement helpers A/B/C near the other Sentry helpers (~:972-:1040 region,
  after _redact_exception_message). Keep them pure (A/B) / side-effect-guarded (C).
  Re-run; all new tests pass.
  </action>
  <verify>
    <automated>cd "C:/Users/juflores/dev/Generate-Weekly-PDFs-DSR-Resiliency" && python -m pytest tests/test_subcontractor_pricing.py -v -k "kpi or snapshot or sentry_log or Telemetry" && python -m py_compile generate_weekly_pdfs.py</automated>
  </verify>
  <done>Three pure helpers exist and are unit-tested incl. explicit PII-safety assertions; zero-duration throughput is 0.0; _sentry_log_event no-ops without DSN and without sentry_sdk.logger; py_compile clean.</done>
</task>

<task type="auto">
  <name>Task 2: Wire helpers into main() - root-txn KPIs (#6), failure attachment (#5), milestone logs (#7)</name>
  <files>generate_weekly_pdfs.py, requirements.txt</files>
  <action>
  #6 - SUCCESS-path root-transaction KPIs. Immediately BEFORE the success
  `if _txn: _txn.set_status("ok")` block (~:10206), add:
      if _txn:
          for _k, _v in _build_run_kpis(
              files_generated=generated_files_count,
              groups_total=len(groups),
              groups_skipped=_groups_skipped,
              groups_generated=_groups_generated,
              groups_uploaded=_groups_uploaded,
              groups_errored=_groups_errored,
              duration_seconds=session_duration.total_seconds(),
              sheets_discovered=len(source_sheets) if 'source_sheets' in dir() else 0,
              rows_fetched=len(all_rows) if 'all_rows' in dir() else 0,
              api_calls=_api_calls_count,
          ).items():
              _txn.set_data(_k, _v)
  (Match the existing dir()-guard idiom already used at :10081-:10082 / :10255.)

  #7 - milestone structured logs. Add _sentry_log_event(...) calls (counts/booleans only):
    - run start: near where _txn is entered (~:8034) -> "weekly run started",
      test_mode=TEST_MODE, github_actions=GITHUB_ACTIONS_MODE
    - run complete: just after the #6 KPI loop (success path) -> "weekly run complete",
      files_generated=generated_files_count, groups_generated=_groups_generated,
      groups_uploaded=_groups_uploaded, groups_errored=_groups_errored,
      duration_seconds=session_duration.total_seconds()
  Do NOT add one inside hot per-group loops. Two or three calls total.

  #5 - FAILURE-path PII-safe attachment. Inside the `except Exception as e:` at ~:10231,
  in the `if SENTRY_DSN:` block, BEFORE the existing sentry_capture_with_context(...) call,
  attach a counts-only snapshot (fully guarded so it can never mask `e`):
      try:
          _snap = _build_run_context_snapshot(
              success=False,
              duration_seconds=session_duration.total_seconds(),
              groups_attempted=len(groups) if 'groups' in dir() else 0,
              groups_generated=_groups_generated,
              groups_uploaded=_groups_uploaded if '_groups_uploaded' in dir() else 0,
              groups_errored=_groups_errored,
              error_type=type(e).__name__,
          )
          scope.add_attachment(
              bytes=json.dumps(_snap, indent=2).encode("utf-8"),
              filename="run-context.json",
              content_type="application/json",
          )
      except Exception:
          pass  # telemetry must never mask the real failure
  (`json` is already imported at module top; `scope` is the get_isolation_scope() already
  obtained at :10238.)

  requirements.txt: change `sentry-sdk>=2.35.0` to `sentry-sdk>=2.54.0` (the structured
  sentry_sdk.logger API floor per RESEARCH section 3). No other dependency changes.

  Do NOT add set_measurement. Do NOT change SENTRY_ENABLE_LOGS default. Do NOT touch the
  billing path or plan-01's edits.
  </action>
  <verify>
    <automated>cd "C:/Users/juflores/dev/Generate-Weekly-PDFs-DSR-Resiliency" && python -m py_compile generate_weekly_pdfs.py && python -m pytest tests/ -q</automated>
  </verify>
  <done>Root _txn carries final run KPIs on success via _build_run_kpis; failure path attaches run-context.json (guarded, counts-only); 2-3 _sentry_log_event milestone calls added (no per-group calls); requirements.txt floor is sentry-sdk>=2.54.0; full pytest green; py_compile clean; no set_measurement, no billing-path changes.</done>
</task>

<task type="auto">
  <name>Task 3: Living Ledger entry</name>
  <files>memory-bank/living-ledger.md</files>
  <action>
  APPEND a dated [YYYY-MM-DD HH:MM] entry (real wall-clock) to the BOTTOM of
  memory-bank/living-ledger.md (NOT CLAUDE.md). Read the last few entries to match style.
  Synthesize what/why/how:
    - Added deferred Sentry telemetry (research #5/#6/#7): final run-level KPIs on the root
      transaction (success path), a PII-safe run-context.json attachment on session failure,
      and a guarded structured-log helper (_sentry_log_event) used at non-PII milestones.
    - All via pure, unit-tested helpers (_build_run_kpis / _build_run_context_snapshot /
      _sentry_log_event) so the PII-safety (counts/booleans/redacted only; both add_attachment
      and the logger bypass before_send_log) is test-enforced.
    - Bumped sentry-sdk floor 2.35.0 -> 2.54.0 (structured logger API; still 2.x, no 3.x -
      3.0 development stopped). set_measurement intentionally NOT used (deprecated 2.28.0).
    - Guardrails: additive only; SENTRY_ENABLE_LOGS stays OFF (so milestone logs are no-ops in
      prod until enabled); telemetry calls are try/except-guarded so they never mask a real error.
  The entry must literally contain "Sentry" and "KPI" (or "telemetry") so the verifier matches.
  </action>
  <verify>
    <automated>cd "C:/Users/juflores/dev/Generate-Weekly-PDFs-DSR-Resiliency" && tail -40 memory-bank/living-ledger.md</automated>
  </verify>
  <done>A new dated bottom entry documents the #5/#6/#7 upgrades, the pure-helper TDD/PII approach, and the 2.54.0 floor bump; CLAUDE.md unchanged.</done>
</task>

</tasks>

<success_criteria>
- _build_run_kpis / _build_run_context_snapshot / _sentry_log_event exist, are pure/guarded,
  and are unit-tested incl. explicit PII-safety assertions.
- Root _txn carries final run KPIs on the success path; failure path attaches a counts-only
  run-context.json; 2-3 milestone structured-log calls added (none in per-group loops).
- requirements.txt sentry-sdk>=2.54.0; no set_measurement; no 3.x APIs.
- All telemetry additions are try/except-guarded and never mask the real exception.
- pytest tests/ green; py_compile clean; plan-01 edits and the billing path untouched.
- Dated Living Ledger entry added; CLAUDE.md not modified by this plan.
</success_criteria>

<output>
After completion, append a "## Plan 02" section to
.planning/quick/260603-mmc-fix-missing-old-rates-csv-default-fileno/260603-mmc-SUMMARY.md
(or create 260603-mmc-SUMMARY-02.md). Do NOT commit docs artifacts - the orchestrator commits.
</output>
