---
milestone: v1.0
milestone_name: Subcontractor Rate Logic (+ Phase 2 hotfix line)
audited: 2026-05-26T18:30:00Z
status: tech_debt
scores:
  requirements: 18/18
  phases: 3/3
  integration: 5/5
  flows: 5/5
gaps: {}   # No critical blockers — all requirements satisfied code-side, all integration wired
tech_debt:
  - phase: 01-subcontractor-rate-logic-modification
    items:
      - "Nyquist: no 01-VALIDATION.md exists (MISSING). Run /gsd-validate-phase 01."
      - "Deferred operator-action: first scheduled GHA production run end-to-end (ROADMAP success criterion 5 in-production portion)."
      - "Deferred operator-action: Step B real-data SKIP_UPLOAD price-write (needs SMARTSHEET_API_TOKEN + subcontractor sheet access)."
      - "Deferred operator-action: one-time Supabase apply of billing_audit/schema.sql (pipeline_run.variant column) before first prod run."
  - phase: 01.1-subcontractor-helper-shadow-rescue
    items:
      - "Nyquist: 01.1-VALIDATION.md is draft (nyquist_compliant: false, wave_0_complete: false) → PARTIAL. Run /gsd-validate-phase 01.1."
      - "Frontmatter hygiene: all six 01.1 SUMMARYs have empty requirements_completed; SUB-08..12 declared in PLAN frontmatter instead (cosmetic — VERIFICATION evidence is complete)."
      - "Deferred operator-action: live cron run produces shadow-variant files with NO legacy primary/_Helper_ file for subcontractor WRs."
      - "Deferred operator-action: data-team Supabase deploy of lookup_attribution RPC, then frozen-helper partitioning under a real mid-week foreman swap."
      - "Deferred operator-action: PPP cleanup whitelist deletes pre-existing off-contract attachments (removed_off_contract > 0 first run, 0 after)."
      - "Deferred operator-action: hash-history prune v2 fires once + idempotent (_phase_prune_version=2 persisted)."
  - phase: 02-attribution-bulk-prefetch-historical-claimer-remediation
    items:
      - "Deferred operator-action: deploy billing_audit.lookup_attribution_bulk RPC to live Supabase + NOTIFY pgrst 'reload schema'."
      - "Deferred operator-action: zero-garbage production run (no _NO_MATCH / _Unknown_Foreman for frozen-claimer rows; O(chunks) attribution HTTP, not ~137k)."
      - "Deferred operator-action: remediation dry-run via advanced_options=remediate_claimers:1,remediation_dry_run:1."
      - "Deferred operator-action (human gate D-11): flip SUPABASE_HASH_STORE_AUTHORITATIVE=1 ONLY after items above validate. Currently dormant '0'."
---

# Milestone v1.0 — Subcontractor Rate Logic (+ Phase 2 hotfix line) — Audit

**Audited:** 2026-05-26
**Status:** `tech_debt` — all 18 requirements satisfied code-side, all 5 cross-phase
seams wired, all 5 E2E flows complete. No critical blockers. Accumulated items
(Nyquist coverage + production-validation operator gates) need review before completion.

**Scope:** 3 phases — `01` (Subcontractor Rate Logic Modification, 14 plans),
`01.1` (Helper-Shadow Rescue, 6 plans), `02` (Attribution Bulk-Prefetch +
Historical Claimer Remediation, 6 plans). 26 plans total. MIG-01 and the v1.1
Backend-Migration / Artifact-Explorer requirements are explicitly descoped to a
future milestone and do not gate v1.0.

---

## Definition of Done (from ROADMAP)

> The production Smartsheet → Excel → Smartsheet attachment pipeline ships
> billing-grade Excel reports without regression: two new subcontractor variants
> (`_AEPBillable`, `_ReducedSub`) with dual-target routing and shadow-foreman
> support (Phase 01), the shadow scaffolding actually firing in production with
> per-row claim attribution (Phase 01.1), and the attribution read path made
> outage-safe and history-scale-safe via bulk prefetch so Sub-project E can be
> safely re-activated (Phase 02).

Every code-side criterion is met. The remaining work is **operator-action
production validation** — the pipeline is production-critical and its true
"without regression" proof is the next scheduled GitHub Actions cron run plus
the Supabase deploy steps, none of which a verifier can execute.

---

## Requirements Coverage (3-Source Cross-Reference)

18 requirements in milestone scope: SUB-01..12 (Phases 01/01.1) + SPEC-1..6
(Phase 02). Cross-referenced across VERIFICATION.md status, SUMMARY.md
`requirements_completed` frontmatter, and the v1.0-REQUIREMENTS.md traceability
table.

| REQ | Phase | VERIFICATION | SUMMARY frontmatter | Traceability | Final |
|-----|-------|--------------|---------------------|--------------|-------|
| SUB-01 | 01 | SATISFIED (code-side) | 01-02, 01-03 | `[ ]` Implemented | **satisfied** |
| SUB-02 | 01 | SATISFIED (code-side) | 01-02, 01-03 | `[ ]` Implemented | **satisfied** |
| SUB-03 | 01 | SATISFIED (code-side) | 01-04 | `[ ]` Implemented | **satisfied** |
| SUB-04 | 01 | SATISFIED (code-side) | 01-01, 01-03 | `[ ]` Implemented | **satisfied** |
| SUB-05 | 01 | SATISFIED (code-side) | 01-02, 01-03, 01-04 | `[ ]` Implemented | **satisfied** |
| SUB-06 | 01 | SATISFIED (code-side) | 01-01..01-04 | `[ ]` Implemented | **satisfied** |
| SUB-07 | 01 | SATISFIED (code-side) | 01-05 | `[ ]` Implemented | **satisfied** (Supabase schema apply deferred) |
| SUB-08 | 01.1 | SATISFIED (code-side) | *(empty; PLAN: SUB-08)* | `[ ]` Implemented | **satisfied** (frontmatter-hygiene note) |
| SUB-09 | 01.1 | SATISFIED (code-side) | *(empty; PLAN: SUB-09)* | `[ ]` Implemented | **satisfied** (frontmatter-hygiene note) |
| SUB-10 | 01.1 | SATISFIED (code-side) | *(empty; PLAN: SUB-10)* | `[ ]` Implemented | **satisfied** (frontmatter-hygiene note) |
| SUB-11 | 01.1 | SATISFIED (code-side) | *(empty; PLAN: SUB-11)* | `[ ]` Implemented | **satisfied** (RPC deploy deferred) |
| SUB-12 | 01.1 | SATISFIED (code-side) | *(empty; PLAN: SUB-12)* | `[ ]` Implemented | **satisfied** (frontmatter-hygiene note) |
| SPEC-1 | 02 | SATISFIED (code-side) | 02-05 | *(02-SPEC.md)* | **satisfied** |
| SPEC-2 | 02 | SATISFIED (code-side) | 02-05 | *(02-SPEC.md)* | **satisfied** |
| SPEC-3 | 02 | SATISFIED (code-side) | 02-05 | *(02-SPEC.md)* | **satisfied** |
| SPEC-4 | 02 | SATISFIED (code-side) | 02-06 | *(02-SPEC.md)* | **satisfied** |
| SPEC-5 | 02 | SATISFIED (code-side, dormant) | 02-06 | *(02-SPEC.md)* | **satisfied** (E flip is a human gate) |
| SPEC-6 | 02 | SATISFIED | 02-05, 02-06 | *(02-SPEC.md)* | **satisfied** (986 passed / 29 skipped / 69 subtests) |

**Coverage: 18/18 satisfied code-side. 0 unsatisfied. 0 orphaned.**

Notes:
- All three phases report VERIFICATION status `human_needed` (not `gaps_found`) —
  i.e. code-complete with production-observation items deferred. Under the status
  matrix this maps to **passed (code-side)** for every requirement.
- **MIG-01** is descoped to v1.1 (Phase 2 of the original plan was deferred); it
  is not in milestone scope and is not counted unsatisfied.
- **SPEC-1..6** are absent from the archived `v1.0-REQUIREMENTS.md` traceability
  table because Phase 02 (the "v1.0 hotfix line") post-dates that archive. They
  are tracked in `02-SPEC.md` and the ROADMAP; coverage is confirmed by
  VERIFICATION + SUMMARY frontmatter, so the missing traceability row is a
  documentation lag, not a coverage gap.

---

## Phase Verification Summary

| Phase | Status | Score | Anti-patterns | Code-side gaps |
|-------|--------|-------|---------------|----------------|
| 01 | human_needed | 5/5 ROADMAP + 12/12 review findings | none | none |
| 01.1 | human_needed | 9/9 code-side must-haves | 1 INFO (documented, non-actionable) | none |
| 02 | human_needed | 6/6 must-haves | none | none |

No code-side gaps in any phase. The single anti-pattern (Phase 01.1 hash-prune
`_hk_variant == 'helper'` broad clause) is an intentionally-broad, version-gated,
one-time, file-deletion-free prune — documented in CLAUDE.md `[2026-05-19 23:45]`
and classified INFO/non-actionable by the verifier.

---

## Cross-Phase Integration (gsd-integration-checker)

**5/5 seams WIRED · 0 orphaned · 0 missing · 0 broken flows.** Independently
confirmed against the live `generate_weekly_pdfs.py`, `billing_audit/*`, and
`weekly-excel-generation.yml` — not just the phase docs.

| Seam | Result | Affected REQs |
|------|--------|---------------|
| 1. Phase 01 → 01.1 shadow-variant emission + 3-site identity lockstep + partition guards | WIRED | SUB-05, SUB-09, SUB-10, SUB-12 |
| 2. 01.1 → 02 attribution-reader replacement (per-row → bulk `_attr_map`); sub-helper consumer fed by same map/fallback; D-04 HOLD vs `rpc_missing` separation; **no orphaned per-row call** | WIRED | SPEC-1, SPEC-2, SPEC-3, SUB-11 |
| 3. billing_audit schema ↔ engine (pipeline_run.variant, lookup_attribution, lookup_attribution_bulk, group_content_hash; distinct `with_retry` op ids; **no schema drift**) | WIRED | SUB-07, SUB-11, SPEC-1, SPEC-5 |
| 4. Sub-project E dormancy (`SUPABASE_HASH_STORE_AUTHORITATIVE: '0'` pinned; skip/clean-name/hash-read paths gated behind it) | WIRED (dormant) | SPEC-5 |
| 5. E2E: subcontractor WR mid-week foreman swap (rescue → bulk resolve → partitioned shadow emission → dual-target routing → cleanup whitelist) | WIRED | SUB-03, SUB-05, SUB-08, SUB-09, SUB-10 |

The highest-risk seam (#2, the 01.1 per-row `lookup_attribution` reader being
replaced by Phase 02's bulk path) was specifically checked for an orphaned
per-row call left in the hot loop or an unfed sub-helper consumer — **neither
exists**. `ATTRIBUTION_RESOLUTION_WEEKS` left only 3 dead comment references; no
dead code, no unreachable branches. One informational (non-finding): the
`_sub_primary_claimer_map` build loop is an intentional O(N) in-memory dict-build
(no Supabase I/O), not a per-row-RPC regression.

---

## Nyquist Compliance

Nyquist validation enabled (config absent = enabled). Discovery-only — this audit
does not auto-run `/gsd-validate-phase`.

| Phase | VALIDATION.md | nyquist_compliant | wave_0_complete | Status |
|-------|---------------|-------------------|-----------------|--------|
| 01 | missing | — | — | **MISSING** |
| 01.1 | exists (draft) | false | false | **PARTIAL** |
| 02 | exists (validated) | true | true | **COMPLIANT** |

**Overall: 1 compliant, 1 partial, 1 missing.** To close: run
`/gsd-validate-phase 01` (create the missing strategy) and `/gsd-validate-phase 01.1`
(promote the draft). Phase 02 is already Nyquist-validated.

---

## Tech Debt (Accumulated — Needs Review Before Completion)

No critical blockers, but the following accumulated items warrant a decision:

### Nyquist coverage
- **Phase 01:** no VALIDATION.md → `/gsd-validate-phase 01`.
- **Phase 01.1:** VALIDATION.md is a draft (`nyquist_compliant: false`) → `/gsd-validate-phase 01.1`.

### Deferred operator-action / production-validation items
These are not code defects — every phase verifier classified them `human_needed`.
They are the genuine production-observation gates for a production-critical pipeline:

- **Phase 01:** first scheduled GHA prod run end-to-end; Step B real-data
  SKIP_UPLOAD price-write; one-time Supabase apply of `billing_audit/schema.sql`
  (`pipeline_run.variant`).
- **Phase 01.1:** live cron produces shadow files with no legacy primary/_Helper_
  on sub WRs; data-team `lookup_attribution` RPC deploy + frozen-helper behaviour
  under a real mid-week swap; PPP off-contract cleanup fires; hash-prune v2 fires
  once + idempotent.
- **Phase 02:** deploy `lookup_attribution_bulk` RPC + `NOTIFY pgrst`; zero-garbage
  prod run (O(chunks) HTTP, no `_NO_MATCH`/`_Unknown_Foreman` for frozen-claimer
  rows); remediation dry-run via `advanced_options`; **human-gated D-11 flip of
  `SUPABASE_HASH_STORE_AUTHORITATIVE=1`** (currently dormant `'0'` — safe).

### Documentation hygiene (cosmetic)
- All six Phase 01.1 SUMMARYs have empty `requirements_completed` frontmatter;
  SUB-08..12 are declared in the PLAN frontmatter instead. VERIFICATION evidence
  is complete, so coverage is unaffected — but a future audit's automated 3-source
  check will re-flag this until the SUMMARY frontmatter is backfilled.

**Total: ~14 items across 3 phases (2 Nyquist + ~11 production-validation gates +
1 cosmetic).**

---

## Verdict

`tech_debt`. The milestone is **code-complete and fully wired**: 18/18 requirements
satisfied code-side, 5/5 integration seams confirmed against live source, 5/5 E2E
flows traced, 986 passing tests, zero code-side gaps, zero broken flows. The
accumulated debt is (a) Nyquist coverage on Phases 01 and 01.1, and (b) a set of
production-observation operator gates that are intrinsic to a Smartsheet-driven
billing pipeline and cannot be discharged by a verifier. Notably, Sub-project E
re-activation remains a deliberate, dormant, human-gated step (`'0'`) — exactly the
guardrail that the Phase 02 hotfix line was created to establish after the prior
premature-flip incident.

Proceed to `/gsd-complete-milestone v1.0` to accept the debt and track the
operator gates in the backlog, OR close the Nyquist gaps first via
`/gsd-validate-phase 01` and `/gsd-validate-phase 01.1`.
